# já já.

1. `pnpm install`
2. `pnpm dev` — web em http://localhost:5173; o runner sobe catalogo (:3001), pedidos (:3002) e pagamentos (:3003) num só processo com o bus em memória
3. `pnpm test` — testes de contratos, catálogo e fluxo pedido → pagamento
