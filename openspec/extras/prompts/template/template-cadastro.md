<!--
TEMPLATE DE PROMPT DE CADASTRO — instruções em README.md (nesta pasta).
Antes de executar: substituir todos os placeholders entre chaves duplas, manter (sem os marcadores)
ou apagar cada bloco SE ... FIM SE e apagar todos os comentários HTML, inclusive este.
-->

# Contexto do projeto (ler antes de executar)

- Namespace do monorepo: `@jaja`. Pacote de domínio: `modules/{{module}}` (`@jaja/{{module}}`), que já possui {{AGREGADOS_EXISTENTES}}. Shared: `@mentoria-360/shared` (submódulo em `packages/shared`). Skills em `.claude/skills/*`; padrão de nomes em `.claude/skills/skills-standards.md` (pastas `model/`, `provider/`, `use-case/`, `dto/`; sufixos `.entity.ts`, `.vo.ts`, `.repository.ts`, `.query.ts`, `.use-case.ts`, `.dto.ts`).
- **Cadastro de referência: `{{CADASTRO_REFERENCIA}}`** (prompt {{NN_REFERENCIA}}). Antes de criar cada arquivo, ler o equivalente dele na mesma camada e seguir o mesmo padrão:
  - domínio: `modules/{{module}}/src/{{CADASTRO_REFERENCIA}}/**`, `modules/{{module}}/test/{{CADASTRO_REFERENCIA}}/**` e `modules/{{module}}/test/mock/in-memory-{{CADASTRO_REFERENCIA}}.repository.ts`;
  - backend: `apps/backend/src/modules/{{module}}/{{CADASTRO_REFERENCIA}}.prisma.ts`, `{{CADASTRO_REFERENCIA}}.controller.ts`, `test/{{CADASTRO_REFERENCIA}}.integration.http`, o model em `apps/backend/prisma/models/{{module}}.model.prisma` e a task de seed correspondente em `apps/backend/prisma/seed/tasks/`;
  - frontend: os arquivos com o nome do cadastro de referência em `apps/frontend/src/modules/{{module}}/data/`, `pages/` e `components/`, as rotas em `apps/frontend/src/app/admin/(shell)/{{module}}/` e as funções de rota em `src/shared/navigation/{{module}}-routes.ts`.

  O código é a fonte da verdade do **padrão** (utilitários, nomes auxiliares, tratamento de erros, estados de carregamento, textos de apoio): se divergir de um prompt anterior, seguir o código. Este prompt é a fonte da verdade das **regras de negócio** do novo cadastro.
- **Leitura em CQRS** (skill `module-query-cqrs`): consultas não seguem o caminho dos comandos. Cada query é uma interface `*Query` em `provider/` (DTOs em `dto/`), implementada no adapter Prisma como atributo público tipado e chamada **direto no controller**, sem caso de uso, sem entidade e sem serviço de domínio. Filtros, busca, ordenação, contagens e campos derivados ficam no SQL; o adapter só normaliza a entrada e mapeia as linhas para DTO. Não criar `find-*.use-case.ts` nem teste unitário para as queries: o comportamento delas é coberto pelo `.http`. Os comandos (`save-*`/`delete-*`) continuam pela entidade, pelo repositório e pelos casos de uso.
- VOs disponíveis no shared: `Id`, `Name` (2–100 caracteres), `Alias` (slug `^[a-z0-9]+(?:-[a-z0-9]+)*$`; `Alias.format(texto)` gera o slug a partir de um texto), `Text` (`minLength`/`maxLength`), `Url` (apenas http/https), `Flag` (booleano), `Order` (inteiro ≥ 0), `PositiveInteger`, `NonNegative`. {{VOS_DO_MODULO}} Entidades estendem `Entity` do shared, que já fornece `id`, `createdAt`, `updatedAt`, `deletedAt` e `cloneWith`. `CrudRepository<T>` do shared define `create(entity, tx?)`, `update(entity, tx?)`, `findById(id): Promise<Result<T>>` e `delete(id, tx?)`. Casos de uso implementam `UseCase<Input, Output>`. Os testes do pacote usam jest.
- Backend é ESM (`"type": "module"`): imports relativos com sufixo `.js`; testes com Vitest. `@jaja/{{module}}` é consumido via `dist`: rodar `npm run build --workspace=@jaja/{{module}}` antes de usá-lo no backend e no frontend. `PrismaService` (`apps/backend/src/db/prisma.service.ts`) implementa `TransactionManager` e exporta `PrismaTransactionContext`. Endpoints administrativos usam `@AdminOnly()` (`apps/backend/src/shared/decorators`): `401` sem token, `403` para não administrador. Busca textual em SQL com `folded`/`toPrefixTsQuery` de `apps/backend/src/db/text-search.sql.ts`, compartilhado por todos os módulos do backend.
- Frontend em Next.js 16 (`params` e `searchParams` são `Promise`). Telas protegidas no grupo `apps/frontend/src/app/admin/(shell)`, que já aplica `AdminGuard` e o shell. Módulo `apps/frontend/src/modules/{{module}}` com `data/` flat, `pages/` e `components/`. Chamadas à API via `apiRequest`/`ApiError`/`toErrorMessage` de `src/shared/util/api-client.util.ts`, com o token de `useAuth().session` passado por parâmetro. Estado das listas na URL com `src/shared/util/list-query.util.ts` (`parsePage`, `pageParam`, `buildListHref`) e `src/shared/util/query-string.util.ts` (`toQueryString`, `RouteSearchParams`). Mensagens dos códigos de erro em `src/shared/i18n/messages.pt.ts` e `messages.en.ts` (`getMessage` de `@/shared/i18n`). Componentes em `src/shared/components/ui` (`PageSectionHeader`, `TableCard`, `Table`, `EmptyListState`, `PaginationControls`, `DeleteConfirmationDialog`, `FormSectionLayout`, `FormSkeleton`, `FormErrorMessage`, `Card`, `Input`, `Textarea`, `Checkbox`, `Combobox`, `OrderableObjectList`, `Badge`). Design em `apps/frontend/DESIGN.md`. Menu em `src/shared/navigation/app-modules.ts`: a seção `{{module}}-registrations` do módulo {{MODULO_LABEL}} já contém {{ITENS_MENU_EXISTENTES}}.
- Seeds: `apps/backend/prisma/seed/main.ts` executa a lista `seedTasks` em ordem (`npx prisma db seed -- --only=<task>`, dentro de `apps/backend`, roda um subconjunto). Cada task lê apenas `apps/backend/prisma/seed/data/*.json`, que já está no formato do banco e referencia outros cadastros por chave natural (ex.: `brandSlug`, `parentSlug`). O backend nunca lê `apps/cli`: quando os dados vêm do scraper, a transformação fica no CLI.
<!-- SE: seed -->
- **Dados de referência:** {{DESCRICAO_DADOS}}
<!-- FIM SE: seed -->
<!-- SE: fora-do-escopo -->
- **Fora do escopo desta funcionalidade:** {{FORA_DO_ESCOPO}}
<!-- FIM SE: fora-do-escopo -->

# Negócio

- Criar o agregado `{{entity}}` (skill: module-aggregate). A skill exige `--mode crud|example`: usar `--mode example` e apagar o use case e o teste gerados, mantendo apenas `model/`, `provider/`, `dto/` e o mock in-memory (`modules/{{module}}/test/mock/in-memory-{{entity}}.repository.ts`). Criar `modules/{{module}}/src/{{entity}}/errors.ts` com `{{Entity}}Errors` (objeto `as const` com todos os códigos deste cadastro: {{CODIGOS_ERRO}}) e o tipo `{{Entity}}ErrorCode`, e `modules/{{module}}/src/{{entity}}/index.ts` reexportando `dto`, `errors`, `model`, `provider` e `use-case`. Exportar `./{{entity}}` em `modules/{{module}}/src/index.ts`.

<!-- SE: vo-proprio -->
- Criar os objetos de valor em `modules/{{module}}/src/{{entity}}/model/`: {{LISTA_VOS}} (skill: module-value-object)
<!-- FIM SE: vo-proprio -->

- Criar a entidade `{{Entity}}` em `model/{{entity}}.entity.ts` (`{{Entity}}Props extends EntityProps`) com os atributos:
  - `id` — identificador único (`Id`)
{{ATRIBUTOS}}

  Invariantes da entidade: {{INVARIANTES}}. Regras que dependem do repositório ficam nos casos de uso. Validar com `Result.combine`, expor `create`/`tryCreate`, getters e `toDTO()`; opcionais ausentes viram `null`.<!-- SE: slug --> Expor `static resolveSlug(slug, name)`: slug em branco é derivado do nome com `Alias.format`; é usado pela entidade e pelo caso de uso.<!-- FIM SE: slug --> (skill: module-entity)

- Criar a interface `{{entity}}.repository` em `provider/` estendendo `CrudRepository<{{Entity}}>` e adicionando {{METODOS_REPOSITORIO}} (buscas por chave única retornam `Result<{{Entity}} | null>`, ok com `null` quando não existir). `findById` segue o contrato do shared e falha com `{{ENTITY}}_NOT_FOUND` quando não existir. Registros com `deletedAt` preenchido nunca são retornados. (skill: module-repository)

- Criar os DTOs em `dto/`: `{{entity}}.dto.ts` com `{{Entity}}DTO` ({{CAMPOS_DTO}}, `createdAt`, `updatedAt`) e `{{Entity}}PageDTO` (`items`, `total`, `page`, `pageSize`, `totalPages = ceil(total / pageSize)`)<!-- SE: item-de-lista -->, além de `{{Entity}}ListItemDTO` ({{CAMPOS_LISTA}}), usado nos `items` da página no lugar do DTO completo<!-- FIM SE: item-de-lista -->; e `{{entity}}-filters.dto.ts` com `{{Entity}}FiltersDTO` (`page` a partir de 1, `pageSize`, `search?`, `isActive?`{{FILTROS_EXTRAS}}). (skill: module-dto)

- Criar as interfaces de query em `provider/`, apenas contrato, sem caso de uso de leitura, documentando no comentário de cada interface o comportamento abaixo: (skill: module-query-cqrs)
  - `find-{{entities}}.query.ts` — `Find{{Entities}}Query`, contrato `execute(filter: {{Entity}}FiltersDTO): Promise<Result<{{Entity}}PageDTO>>`, **paginada**. Sem `search`, ordena por {{ORDENACAO}} (ignorando maiúsculas e acentos); com `search`, cada termo deve casar o início de uma palavra em {{CAMPOS_BUSCA}}, melhores resultados primeiro. `isActive` filtra quando definido.{{REGRAS_FILTROS_EXTRAS}}
  - `find-{{entity}}-by-id.query.ts` — `Find{{Entity}}ByIdQuery`, contrato `execute(id: string): Promise<Result<{{Entity}}DTO | null>>`, `null` quando não existir ou estiver excluíd{{o}}.

- Criar o caso de uso `save-{{entity}}.use-case` (`Save{{Entity}}`, entrada `Save{{Entity}}Input`) em `modules/{{module}}/src/{{entity}}/use-case`, que recebe por parâmetro o repositório `{{entity}}`<!-- SE: referencia --> e os repositórios {{REPOSITORIOS_REFERENCIADOS}}<!-- FIM SE: referencia -->. Entrada: `{ id?, {{CAMPOS_ENTRADA}} }`. Fluxo:
  1. Sem `id` → criação com `Id.createUUID()`. `id` malformado → falha do `Id`. `id` válido → `findById`: encontrou → alteração; falhou com `{{ENTITY}}_NOT_FOUND` → criação com o `id` informado; qualquer outra falha é propagada.
  2. Verificações comuns a criação e alteração (na alteração, ignorando {{o}} própri{{o}} {{rotulo}}), nesta ordem:
{{VERIFICACOES}}
  3. Criação: {{PADROES_CRIACAO}}; persistir com `create`. Alteração: aplicar via `cloneWith` (`undefined` mantém o valor atual; `null` ou `''` limpa os opcionais) com `updatedAt: new Date()`<!-- SE: slug -->; slug não enviado mantém o atual, para renomear não quebrar URLs<!-- FIM SE: slug -->; persistir com `update`.
  4. Retornar o `{{Entity}}DTO` via `toDTO()`.

  (skill: module-use-case)

- Criar o caso de uso `delete-{{entity}}.use-case` (`Delete{{Entity}}`, entrada `{ id }`) em `modules/{{module}}/src/{{entity}}/use-case`, que recebe por parâmetro o repositório `{{entity}}`. Fluxo: buscar por id (falha `{{ENTITY}}_NOT_FOUND`), {{VERIFICACOES_EXCLUSAO}} e deletar via soft delete (`deletedAt`, responsabilidade do repositório).<!-- SE: pendente-futuro --> A regra que impede excluir {{o}} {{rotulo}} com registros de `{{DEPENDENTE_FUTURO}}` vinculados será adicionada no prompt que criar esse agregado.<!-- FIM SE: pendente-futuro --> (skill: module-use-case)

<!-- SE: hierarquia -->
- **Hierarquia** (seguir o agregado `category`):
  - atributos `parentId` (opcional, `Id`; `null` na raiz) e `order` (`Order`, padrão `0`, ordem entre irmãos); invariante da entidade: `parentId` não pode ser igual ao próprio `id`;
  - repositório com `findByParentId(parentId: string | null): Promise<Result<{{Entity}}[]>>` (filhos diretos) e `findAll(): Promise<Result<{{Entity}}[]>>`;
  - `{{Entity}}DTO` com `level` (1 a {{PROFUNDIDADE_MAXIMA}}) e `path` (nomes dos ancestrais e o próprio, separados por " / "), calculados pelas queries no SQL e nunca persistidos; a listagem ordena por `path`;
  - `save-{{entity}}`: o registro pai informado deve existir (falha `PARENT_{{ENTITY}}_NOT_FOUND`); a profundidade resultante do registro salvo **e da sua subárvore** não pode passar de {{PROFUNDIDADE_MAXIMA}} (falha `{{ENTITY}}_MAX_DEPTH_EXCEEDED`); na alteração, o novo pai não pode ser o próprio registro nem um descendente dele (falha `{{ENTITY}}_CYCLE`);
  - `delete-{{entity}}`: falha com `{{ENTITY}}_HAS_CHILDREN` quando houver filhos não excluídos.
<!-- FIM SE: hierarquia -->

<!-- SE: itens-filhos -->
- **Lista de {{ITENS_FILHOS}}** (seguir `ProductImage` no agregado `product`): VO `{{ITEM_FILHO_VO}}` em `model/` com {{CAMPOS_ITEM_FILHO}} e `order` (`Order`); a entidade aceita de 0 a {{MAX_ITENS_FILHOS}} itens e normaliza `order` para a sequência 0..n-1; `{{Entity}}DTO` expõe a lista como DTO próprio; `create`/`update` do repositório gravam a lista junto com {{o}} {{rotulo}} e, na alteração, `save-{{entity}}` substitui a lista inteira pela recebida.
<!-- FIM SE: itens-filhos -->

<!-- SE: referencia -->
- **Referências a cadastros existentes ({{REFERENCIAS}}):**
  - `save-{{entity}}` verifica que cada referência informada existe (falhas {{CODIGOS_REFERENCIA_NAO_ENCONTRADA}}), antes das verificações de unicidade;
  - o repositório `{{entity}}` ganha, para cada cadastro referenciado, `existsBy<Ref>Id(<ref>Id: string): Promise<Result<boolean>>` (considera apenas {{rotulos}} não excluíd{{o}}s);
  - **completar as regras pendentes dos cadastros referenciados:** `delete-<ref>.use-case` passa a receber também o repositório `{{entity}}` e falha com `<REF>_HAS_{{ENTITIES}}` quando `existsBy<Ref>Id` for verdadeiro (depois das verificações que já existem). Adicionar esses códigos ao `errors.ts` de cada cadastro referenciado e atualizar os testes existentes desses casos de uso. {{REGRAS_PENDENTES_ANTERIORES}}
<!-- FIM SE: referencia -->

- Ajustar o mock `test/mock/in-memory-{{entity}}.repository.ts` para espelhar as restrições do banco: chaves únicas valem também para registros excluídos; `create` com id já existente falha com `{{ENTITY}}_NOT_FOUND`; `update` e `delete` de registro inexistente ou excluído falham com `{{ENTITY}}_NOT_FOUND`; buscas ignoram registros excluídos.

- Criar testes unitários (jest, em `modules/{{module}}/test/{{entity}}/`) para a entidade (`{{entity}}.entity.test.ts`: {{CASOS_ENTIDADE}}) e para os casos de uso (`save-{{entity}}.use-case.test.ts` e `delete-{{entity}}.use-case.test.ts`: {{CASOS_USE_CASE}}), usando os mocks in-memory<!-- SE: vo-proprio -->, e um arquivo `<nome>.vo.test.ts` por VO<!-- FIM SE: vo-proprio -->. As queries não têm teste unitário (são cobertas pelo `.http` do backend). Rodar `npm test --workspace=@jaja/{{module}}` e `npm run build --workspace=@jaja/{{module}}`.

> Os passos dos casos de uso podem gerar erros e parar o processo.

# Backend

- Mapear a entidade em `apps/backend/prisma/models/{{module}}.model.prisma`: model `{{Entity}}`, tabela `{{entity_table}}` (`@@map`), `id String @id @db.Uuid`, colunas em snake_case via `@map`, `createdAt @default(now())`, `updatedAt @updatedAt`, `deletedAt` opcional, {{DETALHES_MODEL}}. Comentar acima do model que as chaves únicas continuam reservadas para registros excluídos e qual chave o seed usa no `upsert`.<!-- SE: hierarquia --> Auto-relação `parent`/`children` por `parentId` opcional com `onDelete: Restrict` e índice em `parentId`.<!-- FIM SE: hierarquia --><!-- SE: itens-filhos --> Model filho `{{ITEM_FILHO_MODEL}}` (tabela própria, FK com `onDelete: Cascade`, `@@unique([<fk>, order])`).<!-- FIM SE: itens-filhos --><!-- SE: referencia --> Relações com os models referenciados com `onDelete: Restrict`, índice em cada FK e os campos de relação inversa nos models referenciados.<!-- FIM SE: referencia --> (skill: backend-prisma-data)
- Executar `npm run prisma:migrate:dev --workspace=@jaja/backend -- --name {{module}}_{{entity_snake}}` e `npm run prisma:generate --workspace=@jaja/backend`.
- Criar `apps/backend/src/modules/{{module}}/{{entity}}.prisma.ts` (`{{Entity}}Prisma`, `@Injectable()`, `implements {{Entity}}Repository`) no padrão de `{{CADASTRO_REFERENCIA}}.prisma.ts`:
  - métodos do repositório com `Result.tryAsync`, `toDomain`/`fromDomain` e o client da transação quando receber `tx` (`(tx as PrismaTransactionContext | undefined)?.client ?? this.prisma.client`); `delete` faz soft delete (preenche `deletedAt`); todas as buscas filtram `deletedAt: null`; id que não é uuid não vai ao banco (`findById` falha com `{{ENTITY}}_NOT_FOUND`, as queries retornam `null`);
  - violações de chave única (`P2002`) mapeadas por constraint para o código de domínio (`UNIQUE_VIOLATIONS`): {{MAPA_UNIQUE}} e `{{entity_table}}_pkey` → `{{ENTITY}}_NOT_FOUND` (criação com o id de um registro excluído);<!-- SE: itens-filhos -->
  - `create`/`update` gravam {{o}} {{rotulo}} e a lista de {{ITENS_FILHOS}} na mesma operação (nested write; na atualização, `deleteMany` + `createMany`), seguindo `ProductPrisma`;<!-- FIM SE: itens-filhos -->
  - queries implementadas na mesma classe, cada uma exposta como atributo público tipado com a interface correspondente (`find{{Entities}}: Find{{Entities}}Query`, `find{{Entity}}ById: Find{{Entity}}ByIdQuery`), mapeando o resultado do banco direto para DTO (sem passar pela entidade nem por caso de uso). Filtros, ordenação, contagens e campos derivados ficam no SQL; o adapter só normaliza a entrada e mapeia as linhas. A listagem usa `count` + `LIMIT/OFFSET`; a busca textual segue `BrandPrisma.findBrands` (SQL com documento `tsvector` de peso `A` para {{CAMPOS_PESO_A}} e `B` para {{CAMPOS_PESO_B}}, `toPrefixTsQuery`, `ts_rank` quando há busca) e a ordenação usa `{{COLUNA_ORDEM}} COLLATE "pt-BR-x-icu", id` como desempate estável.<!-- SE: hierarquia --> `level` e `path` são calculados no próprio SQL, como no `BASE_SELECT` de `CategoryPrisma` (self-joins para profundidade fixa ou `WITH RECURSIVE`), sem carregar a tabela para montar a árvore em memória.<!-- FIM SE: hierarquia -->{{DETALHES_QUERY}} Os auxiliares de busca vêm de `apps/backend/src/db/text-search.sql.ts` (`import { folded, toPrefixTsQuery } from '../../db/text-search.sql.js'`);
  - registrar `{{Entity}}Prisma` em `providers` e `exports` de `apps/backend/src/modules/{{module}}/{{module}}.module.ts`.
- Criar o `{{entity}}.controller` em `apps/backend/src/modules/{{module}}/{{entity}}.controller.ts`, decorado com `@AdminOnly()` na classe (todos os endpoints exigem JWT de administrador), e registrá-lo em `controllers` de `{{module}}.module.ts`: (skill: backend-controller)
  - `POST /{{entities}}` — criar, instanciando `Save{{Entity}}` sem `id` (um `id` no corpo é descartado); responde `201` com o `{{Entity}}DTO`
  - `GET /{{entities}}` — lista paginada com `?page=&pageSize=&search=&isActive={{QUERY_EXTRAS}}`, chamando `find{{Entities}}` diretamente no método (sem caso de uso); `page` padrão 1, `pageSize` padrão 20 e máximo 100 (valores inválidos voltam ao padrão); `isActive` só é aplicado para `"true"`/`"false"`; `search` com trim
  - `GET /{{entities}}/:id` — `find{{Entity}}ById` diretamente; `404` com `[{{ENTITY}}_NOT_FOUND]` quando `null`
  - `PUT /{{entities}}/:id` — atualizar, instanciando `Save{{Entity}}` com o `id` da rota (prevalece sobre um `id` no corpo); `200` com o `{{Entity}}DTO`
  - `DELETE /{{entities}}/:id` — exclusão lógica, instanciando `Delete{{Entity}}`; responde `204`

  O corpo é tipado (`Save{{Entity}}Body`) e passa por `toInput`, que repassa ao caso de uso apenas os campos conhecidos. Mapeamento de falhas (códigos sem repetição): {{CODIGOS_404}} → `NotFoundException`; {{CODIGOS_409}} → `ConflictException`; demais falhas do `Result` → `BadRequestException` com os códigos de erro.
<!-- SE: referencia -->
- Atualizar os controllers dos cadastros referenciados ({{REFERENCIAS}}) para instanciar `delete-<ref>.use-case` também com `{{Entity}}Prisma`, mapeando `<REF>_HAS_{{ENTITIES}}` → `ConflictException`.
<!-- FIM SE: referencia -->
<!-- SE: seed -->
- Criar a task de seed `apps/backend/prisma/seed/tasks/{{module}}-{{entities}}.seed.ts` (exportando `seed{{Module}}{{Entities}}`) que lê `apps/backend/prisma/seed/data/{{entities}}.json` resolvendo o caminho a partir de `import.meta.url` (`resolve(dirname(fileURLToPath(import.meta.url)), '../data/{{entities}}.json')`; falhar com mensagem clara se o arquivo não existir) e faz `upsert` por `{{CHAVE_NATURAL}}` com `id` uuid v4 (`randomUUID()`) na inserção e `update: {}` (só insere: não sobrescreve edições do administrador nem traz de volta registros excluídos). {{REGRAS_SEED}} Registrar no array `seedTasks` de `apps/backend/prisma/seed/main.ts` com `name: '{{module}}-{{entities}}'`, após {{SEED_ANTERIOR}}.
<!-- SE: seed-cli -->
- O arquivo `apps/backend/prisma/seed/data/{{entities}}.json` é gerado pelo CLI: {{REGRAS_CLI}} A transformação fica em `apps/cli` e grava no formato acima; o seed do backend não conhece o CLI nem lê `apps/cli`. Atualizar a tabela de arquivos da seção "Seed do backend" em `apps/cli/README.md`.
<!-- FIM SE: seed-cli -->
- Executar `npm run prisma:seed --workspace=@jaja/backend` duas vezes (o seed deve ser idempotente) e conferir {{CONFERENCIA_SEED}}.
<!-- FIM SE: seed -->
- Criar os testes de integração no padrão Rest Client (VS Code) em `apps/backend/src/modules/{{module}}/test/{{entity}}.integration.http`, no estilo de `{{CADASTRO_REFERENCIA}}.integration.http` (cabeçalho com pré-requisitos; variáveis `@baseUrl`, `@adminEmail = usuario@formacao.dev`, `@userEmail` de um usuário do seed com `admin: false`, `@seedPassword = #Senha123` e `@missingId`; tokens via `POST /auth/login`; nomes com sufixo de timestamp para o arquivo poder ser executado de novo), cobrindo: sem token (401), token de usuário não administrador (403), criação (201), {{CENARIOS_HTTP}}, listagem paginada com `search` (200, conferindo `total` e `totalPages`), busca por id (200 e 404), alteração (200) e exclusão (204, seguida de 404 na busca). Esse arquivo é a cobertura das queries (filtros, busca, paginação, ordenação e `null` → 404). Subir o backend (`npm run dev --workspace=@jaja/backend`, porta 4000) e validar as chamadas.
- Validação: `npm run test --workspace=@jaja/backend`, `npm run lint --workspace=@jaja/backend` e `npm run build --workspace=@jaja/backend` sem erros.

# Frontend

- Adicionar as mensagens de {{CODIGOS_I18N}} em `src/shared/i18n/messages.pt.ts` e `messages.en.ts`, na ordem alfabética das chaves.
- Criar em `apps/frontend/src/modules/{{module}}/data/` (arquivos flat, sem subpastas; atualizar `data/index.ts`), no padrão dos arquivos de `{{CADASTRO_REFERENCIA}}`:
  - `{{entity}}.api.ts` — tipos `{{Entity}}` (DTO da API, datas em ISO), `{{Entity}}Page`, `{{Entity}}Input` (corpo de `POST`/`PUT`; `null` limpa os opcionais) e `{{Entity}}Filter`; funções `list{{Entities}}`, `get{{Entity}}`, `create{{Entity}}`, `update{{Entity}}` e `delete{{Entity}}` sobre `apiRequest`, recebendo o `token` como primeiro parâmetro (hooks não podem ser usados fora de componentes); ids com `encodeURIComponent`; a query string só leva os filtros definidos
  - `{{entity}}.schema.ts` — `{{entityCamel}}Schema` com `v.defineObject` (`@/shared/components/form/validator`) usando os mesmos VOs e limites do domínio ({{CAMPOS_SCHEMA}}), com os limites exportados como constantes; tipo `{{Entity}}FormData` via `v.infer` (skill: frontend-form-schema)
  - `use-{{entities}}.hook.ts` — `use{{Entities}}()`: token de `useAuth().session`; `page`, `search`{{FILTROS_URL}} lidos de `useSearchParams` (a URL é o estado da lista: recarregar ou compartilhar reproduz a mesma página); busca aplicada ~300 ms após a última tecla, voltando à página 1 (`router.replace`); troca de página com `router.push`; `loading` derivado da chave da requisição; página além da última redireciona para a última existente; expõe `listQuery` para os links do formulário; `remove(id)` exibe toaster de sucesso e recarrega (voltando uma página ao excluir o único item de uma página > 1) e, em erro (inclusive `409`), `toast.error(toErrorMessage(error))` mantendo o registro na lista
  - `use-{{entity}}-form.hook.ts` — `use{{Entity}}Form({ id, returnQuery })`: `useForm` com `v.resolver({{entityCamel}}Schema)`; no modo edição carrega por id e faz `reset` (falha vira toaster e volta para a lista); no envio monta o `{{Entity}}Input` (opcionais vazios como `null`; `isActive` apenas na edição){{CONVERSOES_FORM}}, exibe toaster, volta para `listHref` (mesma página e filtros de origem) e mapeia o `ApiError` para erro de campo pelo código (`CONFLICT_FIELD_BY_CODE` + `getMessage`): {{MAPA_ERRO_CAMPO}}; demais erros viram toaster<!-- SE: selecionavel -->
  - `use-{{entity}}-options.hook.ts` — `use{{Entity}}Options({ selectedId, selectedLabel })` no padrão de `use-brand-options.hook.ts` (busca na API com debounce, "Carregar mais" por páginas e rótulo da opção selecionada mesmo fora das páginas carregadas), para os formulários e filtros dos cadastros que vão referenciar {{o}} {{rotulo}}<!-- FIM SE: selecionavel --><!-- SE: hierarquia -->
  - `{{entity}}.util.ts` — `build{{Entity}}Tree(items)` (lista plana → árvore ordenada por `order` e nome) e `getDescendantIds(items, id)`, seguindo `category.util.ts`<!-- FIM SE: hierarquia -->
- Criar a página `{{entities}}.page.tsx` em `apps/frontend/src/modules/{{module}}/pages` (`{{Entities}}Page` e `{{Entities}}PageSkeleton`): `PageSectionHeader` com título "{{Rotulos}}", subtítulo com a contagem (total no cadastro ou "N {{rotulos}} encontrad{{o}}s" durante a busca) e botão "{{Novo}} {{rotulo}}" (link levando `listQuery`); campo de busca (`role="search"`){{FILTROS_TELA}}; esqueleto estático na primeira carga e opacidade reduzida com `aria-busy` nas seguintes; lista seguida de `PaginationControls` (`page`, `totalPages`, `totalItems`, `totalLabel: "{{rotulos}}"`). Rota: `/admin/{{module}}/{{entities}}`.
- Criar o componente `{{entity}}-list.component.tsx` em `apps/frontend/src/modules/{{module}}/components`, que renderiza a lista recebida como prop (`TableCard` + `Table`) com {{COLUNAS_LISTA}}, status (badge "Ativ{{o}}"/"Inativ{{o}}") e ações de editar (link levando `listQuery`) e excluir (`DeleteConfirmationDialog`); `EmptyListState` quando vazia, com título e subtítulo próprios durante a busca.
<!-- SE: hierarquia -->
- A lista é exibida como **árvore** (seguir `categories.page.tsx` e `category-tree.component.tsx`, criando `{{entity}}-tree.component.tsx` no lugar de `{{entity}}-list.component.tsx`): recuo proporcional ao `level`, recolher/expandir por nó, ação "{{Novo}} sub{{rotulo}}" nas linhas com `level` menor que {{PROFUNDIDADE_MAXIMA}} (abre a criação com `?parentId=`) e `DeleteConfirmationDialog` com `confirmDisabled` + `confirmDisabledMessage` quando o nó tem filhos. No formulário, o campo `parentId` (`Combobox` com a opção "Sem {{rotulo}} pai (raiz)") oferece apenas registros com `level` menor que {{PROFUNDIDADE_MAXIMA}}, excluindo o próprio e seus descendentes, rotulados pelo `path`; `PARENT_{{ENTITY}}_NOT_FOUND`, `{{ENTITY}}_MAX_DEPTH_EXCEEDED` e `{{ENTITY}}_CYCLE` viram erro do campo `parentId`.
<!-- FIM SE: hierarquia -->
- Criar o componente `{{entity}}-form.component.tsx` em `apps/frontend/src/modules/{{module}}/components` com suporte aos fluxos de criação e edição. O formulário **não deve ser implementado via modal**: `<form noValidate>` com seções `FormSectionLayout` dentro de `Card`, recebendo o `form` do hook (`react-hook-form` + `v.resolver({{entityCamel}}Schema)`), `FormErrorMessage` por campo e botões salvar/cancelar (`cancelHref`). Seções e campos: {{SECOES_FORM}}.<!-- SE: slug --> O `slug` é preenchido a partir do nome com `Alias.format` enquanto o usuário não o editar manualmente; na edição, já começa como editado.<!-- FIM SE: slug --><!-- SE: itens-filhos --> A lista de {{ITENS_FILHOS}} usa `OrderableObjectList` (mover, remover, adicionar; a primeira é a principal) e, no schema, `v.defineArray` com no máximo {{MAX_ITENS_FILHOS}} itens, seguindo `product-form.component.tsx`.<!-- FIM SE: itens-filhos --><!-- SE: referencia --> As referências usam `Combobox` alimentado pelos hooks de opções existentes ({{HOOKS_OPCOES}}); criar o que faltar no padrão de `use-brand-options.hook.ts`.<!-- FIM SE: referencia --> `isActive` (`Checkbox`) aparece apenas no fluxo de edição. Página `{{entity}}-form.page.tsx` em `modules/{{module}}/pages` (`{{Entity}}FormPage({ id, returnQuery })`), com `PageSectionHeader` ("{{Novo}} {{rotulo}}" / "Editar {{rotulo}}", link "← Voltar para {{rotulos}}") e `FormSkeleton` enquanto carrega, usada pelas rotas `/admin/{{module}}/{{entities}}/new` e `/admin/{{module}}/{{entities}}/[id]`.
- Criar as rotas **dentro do grupo `(shell)`**:
  - `apps/frontend/src/app/admin/(shell)/{{module}}/{{entities}}/page.tsx` — renderiza `{{Entities}}Page` dentro de `<Suspense fallback={<{{Entities}}PageSkeleton />}>` (a página usa `useSearchParams`)
  - `(shell)/{{module}}/{{entities}}/new/page.tsx` — aguarda `searchParams` e repassa `returnQuery={toQueryString(...)}`<!-- SE: hierarquia --> e o `parentId` pré-selecionado<!-- FIM SE: hierarquia -->
  - `(shell)/{{module}}/{{entities}}/[id]/page.tsx` — aguarda `params` e `searchParams` e renderiza `<{{Entity}}FormPage key={id} id={id} returnQuery={...} />`
- Em `src/shared/navigation/{{module}}-routes.ts`, criar `{{MODULE}}_{{ENTITIES}}_ROUTE`, `{{module}}{{Entities}}Route(query?)`, `{{MODULE}}_{{ENTITY}}_NEW_ROUTE`, `{{module}}{{Entity}}NewRoute(query?)` e `{{module}}{{Entity}}Route(id, query?)`, todas com `withQuery`<!-- SE: novo-modulo-menu --> (se o arquivo ainda não tiver `withQuery`, extraí-lo de `catalog-routes.ts` para um util compartilhado em `src/shared/navigation/` e importar nos dois arquivos, sem duplicar)<!-- FIM SE: novo-modulo-menu -->. Atualizar `apps/frontend/src/modules/{{module}}/index.ts` com os componentes e as páginas novos.
- Adicionar o item **"{{Rotulos}}"** (`id: '{{module}}-{{entities}}'`, ícone `{{ICONE}}` do `lucide-react`, `href: {{MODULE}}_{{ENTITIES}}_ROUTE`, `match: 'prefix'`) na seção `{{module}}-registrations` de `sectionsByModuleId.{{module}}` em `src/shared/navigation/app-modules.ts`, {{POSICAO_MENU}}.<!-- SE: novo-modulo-menu --> O módulo ainda não tem seções: criar, como no módulo Catálogo, `{ id: '{{module}}-overview', items: [{ id: '{{module}}-overview', label: 'Visão geral', href: {{MODULE}}_ROUTE, match: 'exact' }] }` e `{ id: '{{module}}-registrations', items: [...] }`.<!-- FIM SE: novo-modulo-menu -->
<!-- SE: referencia -->
- Nas telas dos cadastros referenciados ({{REFERENCIAS}}), a exclusão bloqueada (`409 <REF>_HAS_{{ENTITIES}}`) deve exibir `toast.error` com a mensagem traduzida e manter o registro na lista; conferir que o `remove` dos hooks existentes já faz isso e ajustar se não fizer. Os filtros por referência da lista, quando houver, usam os mesmos hooks de opções do formulário.
<!-- FIM SE: referencia -->
- Em caso de sucesso em qualquer operação (criar, editar, excluir), exibir toaster de sucesso ("{{Rotulo}} criad{{o}}", "{{Rotulo}} atualizad{{o}}", "{{Rotulo}} excluíd{{o}}"), voltar para a lista preservando a query string (página, busca e filtros) e atualizar a lista.
- Validação: `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend` sem erros; conferir no navegador, logado como `usuario@formacao.dev`, {{CONFERENCIA_NAVEGADOR}}, e que os cadastros já existentes ({{ITENS_MENU_EXISTENTES}}) continuam funcionando.

> Obs: IMPORTANTE!!! Executar as três partes (Negócio, Backend e Frontend) em subagentes separados com contexto limpo em cada um deles, de forma sequencial (Backend e Frontend dependem do build de `@jaja/{{module}}`). Cada subagente deve ler `.claude/skills/skills-standards.md` e, antes de criar seus arquivos, os equivalentes do cadastro de referência (`{{CADASTRO_REFERENCIA}}`) na sua camada; o do frontend deve ler também `apps/frontend/DESIGN.md`. Uma parte só começa depois de a anterior terminar com as validações passando, e cada subagente encerra listando os arquivos criados/alterados e o resultado das validações.
