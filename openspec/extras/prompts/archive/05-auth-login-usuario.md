# Contexto do projeto (ler antes de executar)

- Namespace do monorepo: `@jaja`. Pacote de domínio: `modules/auth` (`@jaja/auth`). Shared: `@mentoria-360/shared` (submódulo em `packages/shared`, **não alterar**). Skills em `.claude/skills/*` (submódulo, não alterar).
- **Cenário de e-commerce.** A vitrine (`/`, `/p/[slug]`) é pública. O cliente já pode entrar ou criar conta pelo controle "entrar" do cabeçalho, que leva à rota pública `/entrar` (funcionalidade 03). A área administrativa (`/admin`, `/admin/login`, `AdminGuard`) tem login próprio e dashboard; **não alterar** nada do fluxo administrativo.
- O que já existe e deve ser **reaproveitado** (nada disso é recriado aqui):
  - Backend: `POST /auth/register` (201 sem corpo, sempre `admin = false`), `POST /auth/login` (200 com `{ token, user }`), `GET /auth/me`. Casos de uso `CreateUser` e `AuthenticateUser` em `modules/auth/src/app/use-case` (movidos na 04). Backend sobe com `npm run dev --workspace=@jaja/backend` na porta 4000; banco com `npm run db:start --workspace=@jaja/backend` (porta 5433) e seed com `npm run prisma:seed --workspace=@jaja/backend`.
  - Testes de integração: `apps/backend/src/modules/auth/test/auth.integration.http` (Rest Client) já tem 15 cenários; o passo 1 (`# @name register`, email `{{newEmail}}` com sufixo de timestamp) seguido do passo 8 (login com o mesmo `{{newEmail}}` -> 200 com `user.admin = false`) **já cobre** "registro do cliente seguido de login com as mesmas credenciais".
  - Frontend, `apps/frontend/src/modules/auth` (tudo exportado em `index.ts` e `data/index.ts`): `data/auth.types.ts` (`AuthUser`, `AuthSession`, `RegisterInput`), `data/auth.api.ts` (`login`, `register`; 409 vira "Este email já está cadastrado"), `data/login.schema.ts`, `data/register.schema.ts`, `data/auth-storage.ts` (cookie `jaja_auth`), `data/auth.context.tsx` (`AuthProvider` montado no root layout `src/app/layout.tsx` junto com o `<Toaster />`; `useAuth` expõe `session`, `user`, `isAuthenticated`, `isAdmin`, `signIn`, `signUp`, `signOut`), `data/first-name.util.ts` (`firstNameOf`), `components/login-form.component.tsx` e `register-form.component.tsx` (ambos com `onSuccess(user)`), `components/auth-form.component.tsx` (`AuthForm` com abas "Entrar"/"Criar conta", props `defaultMode?: AuthMode` e `onSignedIn(user: AuthUser, mode: AuthMode)`), `pages/storefront-login.page.tsx` (`StorefrontLoginPage`, rota `/entrar`; exporta também `resolveReturnTo`; usa `useEffect` para redirecionar quem já tem sessão para `voltar` — o checkout **não** deve copiar esse redirect).
  - Padrão visual da página `/entrar`, a ser repetido no checkout: `<main className="flex flex-1 flex-col px-6 py-8 md:py-10">`, título `<h1 className="font-display text-3xl font-extrabold leading-tight tracking-tight md:text-5xl">…<span className="text-brand">.</span></h1>` (ponto final em vermelho), conteúdo dentro de `Suspense` com um skeleton estático em `text-placeholder`/`border-placeholder` (sem shimmer). Toasts via `toast` de `sonner`; mensagem de boas-vindas já usada em `/entrar`: "Bem-vindo, {primeiro nome}" no login e "Conta criada. Bem-vindo, {primeiro nome}" no registro.
  - Loja: `src/modules/catalog/components/storefront-shell.component.tsx` (`StorefrontShell`) é aplicado a todas as rotas de `src/app/(public)/` pelo `(public)/layout.tsx`. Dentro dele, `ConnectedHeader` (em `Suspense`, porque usa `useSearchParams`) lê `useStorefront()` e `useAuth()`, monta o `signInHref` (`/entrar?voltar=<url atual>`), passa `userName`/`signInHref`/`onSignOut` ao `StorefrontHeader` (que já mostra "entrar" ou "olá, nome" + "sair"; o `onSignOut` chama `signOut()` e `toast.success('Até já já.')`) e passa ao `BagSheet` `onChangeQuantity={noop}` e `onCheckout={noop}`. O botão "Fechar pedido" do `BagSheet` (`src/shared/components/store/bag-sheet.component.tsx`) chama `onCheckout`. A sacola ainda é vazia e sem persistência (carrinho é outra funcionalidade).
  - Query da vitrine: `useStorefront()` (`src/modules/catalog/data/use-storefront.hook.ts`) expõe `neighborhood`, `category`, `closeBag`, etc.; `buildStorefrontQuery(neighborhood, category)` do mesmo arquivo gera `bairro=…&categoria=…`. Rotas da loja em `src/shared/navigation/storefront-routes.ts`: `STOREFRONT_ROUTE = '/'` e `STOREFRONT_LOGIN_ROUTE = '/entrar'` (ainda não existe constante de checkout).
  - Módulo frontend `src/modules/orders/`: `components/orders-dashboard.component.tsx`, `pages/dashboard.page.tsx` (`DashboardPage`, usado em `/admin/orders`), `data/index.ts` (só `export {}`) e `index.ts` com `export *` dos três. Páginas de rota em `src/app/**/page.tsx` são wrappers de uma linha que renderizam a página do módulo (ver `src/app/(public)/entrar/page.tsx`).
  - Textos da loja são hardcoded em pt-BR nos componentes (o `src/shared/i18n` existe, mas nenhum componente de loja/auth o usa). Não introduzir i18n no checkout.
- Toda tela segue `apps/frontend/DESIGN.md` (flat, radius 0, sem sombras, divisores 2px, vermelho só em pontos de destaque, botão primário tinta sobre papel, carregando com réguas em `#B0ADA4`, **nada anima ao carregar**). Não há seção de checkout no `DESIGN.md`; seguir as regras gerais e o padrão de `/entrar`. Ler o arquivo antes de escrever qualquer tela.

# Negócio

- Sem alterações no pacote `@jaja/auth`.

# Backend

- Sem novos endpoints e sem novo cenário no `.http`: o fluxo "registro do cliente seguido de login com as mesmas credenciais" já é coberto pelos passos 1 e 8 de `apps/backend/src/modules/auth/test/auth.integration.http`. Apenas subir banco e backend e reexecutar os passos 1 e 8 confirmando 201 e depois 200 com `user.admin = false`. Se quiser deixar isso explícito, acrescentar um comentário no cabeçalho do passo 8 apontando para o passo 1; nenhuma outra edição.

# Frontend

- Adicionar `CHECKOUT_ROUTE = '/checkout'` em `src/shared/navigation/storefront-routes.ts`.
- Criar a rota pública `/checkout`:
  - `apps/frontend/src/modules/orders/pages/checkout.page.tsx` exportando `CheckoutPage` (adicionar `export * from './pages/checkout.page'` em `modules/orders/index.ts`) e `src/app/(public)/checkout/page.tsx` como wrapper de uma linha, no mesmo padrão de `(public)/entrar/page.tsx`. O `StorefrontShell` já envolve a rota pelo layout público; não montar shell nem `AuthProvider` de novo.
  - A página usa `useAuth()` e `useStorefront()`; como `useStorefront` chama `useSearchParams`, o conteúdo fica em um componente interno dentro de `Suspense` com skeleton estático (mesma técnica de `StorefrontLoginPage`).
  - Sem sessão (`isAuthenticated === false`): título display "Para fechar o pedido, entre ou crie sua conta." com o ponto final em `text-brand`, e o `AuthForm` existente abaixo (dentro de `Card`/`CardContent`, como em `/entrar`). No `onSignedIn(user, mode)`: `toast.success` com a mesma mensagem de `/entrar` ("Bem-vindo, {primeiro nome}" ou "Conta criada. Bem-vindo, {primeiro nome}", via `firstNameOf`) e **permanecer** em `/checkout`: sem `router.replace`, sem `useEffect` de redirect; a troca de conteúdo acontece porque `isAuthenticated` muda no contexto.
  - Com sessão: placeholder com título display "Checkout chega já já." (ponto final em `text-brand`), nome e email do cliente (`user.name`, `user.email`) e um link "voltar para a vitrine" para `` `${STOREFRONT_ROUTE}?${buildStorefrontQuery(neighborhood, category)}` `` (preserva `bairro` e `categoria`). O checkout real (endereço, pagamento, pedido) fica para funcionalidade futura.
- Ligar a sacola ao checkout em `storefront-shell.component.tsx` (`ConnectedHeader`): o `onCheckout` do `BagSheet` deixa de ser `noop` e faz `storefront.closeBag()` seguido de `router.push(`${CHECKOUT_ROUTE}?${buildStorefrontQuery(storefront.neighborhood, storefront.category)}`)` (`useRouter` de `next/navigation`). Manter o `noop` em `onChangeQuantity`. Não bloquear pela sacola vazia nesta funcionalidade (o carrinho ainda não existe).
- Nenhuma proteção de rota na loja: `/checkout` é pública e apenas alterna o conteúdo conforme a sessão. `/entrar`, o cabeçalho, `AdminGuard` e `/admin/*` permanecem intactos. "sair" no cabeçalho já chama `signOut()`; em `/checkout` isso deve voltar o conteúdo ao formulário sem trocar de rota.
- Validação: `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend` sem erros; com banco e backend no ar, subir `npm run dev --workspace=@jaja/frontend` e conferir: "Fechar pedido" na sacola fecha a sacola e leva a `/checkout?bairro=…&categoria=…` com as abas Entrar/Criar conta; criar conta com dados válidos deixa o cliente logado no placeholder do checkout (mesma URL) e o cabeçalho mostra "olá, Nome"; senha fraca, confirmação divergente e email já cadastrado são barrados com as mensagens já existentes; "sair" no cabeçalho volta o checkout ao formulário; reload em `/checkout` mantém a sessão; "voltar para a vitrine" leva a `/?bairro=…&categoria=…` com o mesmo bairro; `/entrar` e `/admin/login` continuam funcionando como antes.

> Obs: IMPORTANTE!!! Só o Frontend tem trabalho de código; executar em um subagente com contexto limpo, que deve ler `apps/frontend/DESIGN.md` e `src/modules/auth/pages/storefront-login.page.tsx` antes de escrever qualquer arquivo. A validação do Backend (passos 1 e 8 do `.http`) pode ser feita no mesmo subagente, antes da validação visual do Frontend.
