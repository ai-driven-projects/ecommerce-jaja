# Contexto do projeto (ler antes de executar)

- Namespace do monorepo: `@jaja`. Pacote de domínio: `modules/auth` (`@jaja/auth`). Shared: `@mentoria-360/shared` (submódulo em `packages/shared`, **não alterar**: qualquer tipo novo fica no backend/frontend).
- As skills ficam em `.claude/skills/*` (submódulo). Os scripts das skills mencionam `.agents/skills/...`; usar `.claude/skills/...` no lugar.
- Padrão de nomes: `../.claude/skills/skills-standards.md` (pastas `model/`, `provider/`, `use-case/`, `dto/`; sufixos `.entity.ts`, `.repository.ts`, `.use-case.ts`, `.vo.ts`).
- VOs disponíveis no shared: `Id`, `PersonName`, `Name`, `Email`, `Url`, `Flag` (boolean), `Password` (valida **hash bcrypt**), `StrongPassword` (valida força). Não existem `HashPassword` nem `URL`.
- Base para transação: `TransactionManager`/`TransactionContext` do shared. No backend, `PrismaService` (`apps/backend/src/db/prisma.service.ts`) já implementa `TransactionManager` e entrega `{ client }` no contexto.
- `modules/auth` hoje contém apenas o scaffold placeholder `src/auth` (entidade `Auth`, `CreateAuth`, `AuthDTO`, mock e teste). Ele deve ser removido nesta funcionalidade.
- Backend é ESM (`"type": "module"`): imports relativos com sufixo `.js`.
- `@jaja/auth` é consumido pelo backend/frontend via `dist`: rodar `npm run build --workspace=@jaja/auth` antes de usar no backend.
- Infra de JWT já existe no backend (`apps/backend/src/shared`): `SharedModule` (global) registra `JwtModule` (segredo `JWT_SECRET`, expiração 7d) e exporta `JwtService`; `JwtStrategy` valida o Bearer token e mapeia o payload (`JwtPayload` = `{ sub, name, email }`) para `AuthenticatedUser` (`{ id, name, email }`, do shared); `JwtGuard` respeita `@Public()`; `@CurrentUser()` lê `request.user`. O `JwtGuard` **não** está registrado como guard global.
- **Cenário de e-commerce.** A vitrine (`/`, `/p/[slug]`) é pública e continua pública: ninguém precisa se logar para navegar, filtrar ou montar a sacola. O cliente **pode** entrar ou criar conta quando quiser pelo controle "entrar" do cabeçalho da loja, que leva à rota pública `/entrar`; o checkout (funcionalidade 05) apenas reaproveita esse formulário. Toda a parte administrativa (cadastros, dashboards) vive na rota **`/admin`**, que exige usuário logado **e** administrador; a tela `/admin/login` também oferece "Criar conta", mas quem se registra nasce usuário comum e é avisado de que a área é restrita. O usuário do módulo de autenticação tem a flag `admin`.
- Estado atual do frontend: `src/app/(public)/layout.tsx` envolve tudo em `StorefrontShell` exceto `/auth` (placeholder em `(public)/auth/page.tsx`); `src/app/(private)/layout.tsx` usa `RouteGuard` provisório (`src/shared/auth`) + `ShellProvider` + `AdminShell` (props `userName`, `userEmail`, `userAvatarUrl`, `onLogout`, `logoHref`, `profileHref`) + `AppSidebarNavigation`; as páginas privadas são `/principal`, `/catalog`, `/orders`, `/customers`, `/stores` e `/auth/dashboard` (placeholders "Módulo em desenvolvimento"); as constantes de rota ficam em `src/shared/navigation/*-routes.ts` e o rail/sidebar em `src/shared/navigation/app-modules.ts`; o `<Toaster />` de `@/shared/components/ui/toaster` **não** está montado no `layout.tsx`; `apps/frontend/.env` e `.env.example` já têm `NEXT_PUBLIC_API_URL=http://localhost:4000`.
- Toda tela e componente do frontend segue `apps/frontend/DESIGN.md` (design flat "já já.": radius 0, sem sombras/gradientes, divisores 2px, nada anima ao carregar). Ler antes de escrever qualquer tela.

# Negócio

- Remover o scaffold placeholder `modules/auth/src/auth`, `modules/auth/test/auth` e `modules/auth/test/mock/in-memory-auth.repository.ts`, ajustando `modules/auth/src/index.ts`.
- Criar o agregado de `user` (skill: module-aggregate). A skill exige `--mode crud|example`: usar `--mode example` e apagar o use case e o teste gerados, mantendo apenas `model/`, `provider/`, `dto/` e o mock in-memory (`test/mock/in-memory-user.repository.ts`).
- Alterar a entidade `user` para possuir os atributos: id, name, email, avatarUrl (opcional) e **admin** (boolean, padrão `false`). Validar com os VOs `Id`, `PersonName`, `Email`, `Url` (opcional) e `Flag` via `Result.combine`, expondo `create`/`tryCreate` e getters (`isAdmin`). (skill: module-entity)
- Criar o DTO `UserDTO` em `modules/auth/src/user/dto/user.dto.ts` com `{ id, name, email, avatarUrl, admin }` (nunca inclui senha). (skill: module-dto)
- Criar a interface de `user.repository` estendendo `CrudRepository<User>` e adicionando `findByEmail(email: string): Promise<Result<User | null>>` (ok com `null` quando não existir). (skill: module-repository)

- Criar o agregado de `password` (skill: module-aggregate, mesmo procedimento: `--mode example` e remover o use case gerado).
- Alterar a entidade `password` para possuir os atributos: id, userId, value. O `value` é o **hash** da senha e deve ser validado pelo VO `Password` (que aceita apenas hash bcrypt); `userId` validado com `Id`. A validação de senha forte (`StrongPassword`) NÃO fica na entidade, fica no caso de uso antes de criptografar. (skill: module-entity)
- Criar a interface de `password.repository` com `create(password: Password, tx?: TransactionContext): Promise<Result<void>>` e `findByUserId(userId: string): Promise<Result<Password | null>>`. (skill: module-repository)
- Criar a interface `password-crypto.provider` em `modules/auth/src/password/provider/password-crypto.provider.ts` com `hash(plain: string): Promise<string>` e `compare(plain: string, hash: string): Promise<boolean>`.

- Criar o caso de uso `create-user.use-case.ts` em `modules/auth/src/user/use-case/` (skill: module-use-case). Ele recebe no construtor: `UserRepository`, `PasswordRepository`, `PasswordCryptoProvider` e `TransactionManager`. Entrada: `{ name, email, password, avatarUrl? }` — **não** aceita `admin`: todo usuário registrado nasce com `admin = false` (administradores só existem via seed nesta funcionalidade). Fluxo: verificar se o email já existe via `findByEmail` (falha `EMAIL_ALREADY_EXISTS`), validar nome, validar email, validar senha forte com `StrongPassword`, criptografar a senha, criar a entidade `user`, criar a entidade `password` (com o `userId`), persistir as duas dentro de `transactionManager.runInTransaction`, repassando o `tx` para os `create` dos repositórios.
  > Cada passo pode falhar e interrompe o processo com `Result.fail(...)`.
- Criar o caso de uso `authenticate-user.use-case.ts` também em `modules/auth/src/user/use-case/` (skill: module-use-case). Recebe no construtor `UserRepository`, `PasswordRepository` e `PasswordCryptoProvider`. Entrada: `{ email, password }`. Fluxo: buscar o usuário pelo email, buscar a senha pelo `userId`, comparar a senha informada com o hash via `compare`. Usuário inexistente, senha ausente ou senha incorreta falham **todos** com o mesmo código `INVALID_CREDENTIALS` (não revelar se o email existe). Sucesso devolve `Result.ok<UserDTO>` (com `admin`, nunca a senha).
  > Ambos os casos de uso usam dois agregados; a funcionalidade 04 move os dois para `modules/auth/src/app/use-case`.
- Criar testes unitários (jest, em `modules/auth/test/**`) para as entidades `user` (incluindo `admin` padrão `false` e `admin` inválido) e `password` e para os dois casos de uso (registro com sucesso, email duplicado, senha fraca; login com sucesso retornando `admin`, email inexistente e senha errada com `INVALID_CREDENTIALS`), usando os mocks in-memory, um `PasswordCryptoProvider` fake e um `TransactionManager` fake. Rodar `npm test --workspace=@jaja/auth`.

# Backend

- Antes de tudo: a porta 5432 está ocupada pelo container `poupig-db-postgres` de outro projeto. Conferir que `apps/backend/docker-compose.yml`, `DATABASE_URL` em `apps/backend/.env` e `.env.example` usam a porta `5433` (já ajustado) e subir o banco com `npm run db:start --workspace=@jaja/backend`. Definir um `JWT_SECRET` real no `.env`.
- Instalar `bcrypt` e `@types/bcrypt` em `apps/backend`.
- Mapear a entidade `user` no arquivo já existente `apps/backend/prisma/models/auth.model.prisma` (tabela `users`, email único, coluna `admin Boolean @default(false)`, `@@map`). (skill: backend-prisma-data)
- Mapear a entidade `password` no mesmo arquivo (tabela `passwords`), com relação 1:1 obrigatória com `user` (`userId` único, FK com `onDelete: Cascade`). (skill: backend-prisma-data)
- Executar `npm run prisma:migrate:dev --workspace=@jaja/backend -- --name auth_user_password` e `npm run prisma:generate --workspace=@jaja/backend`.
- Criar a implementação Prisma do repositório de `user` em `apps/backend/src/modules/auth/user.prisma.ts` (`UserPrisma`), com `toDomain`/`fromDomain` (incluindo `admin`), retornando `Result`, e usando `(tx as PrismaTransactionContext)?.client ?? this.prisma.client` quando receber transação.
- Criar a implementação Prisma do repositório de `password` em `apps/backend/src/modules/auth/password.prisma.ts` (`PasswordPrisma`), mesmo padrão.
- Criar a implementação do `password-crypto.provider` em `apps/backend/src/modules/auth/bcrypt.provider.ts` (`BcryptProvider`, salt rounds 10).
- Remover `apps/backend/src/modules/auth/auth.prisma.ts` e registrar `UserPrisma`, `PasswordPrisma` e `BcryptProvider` como providers em `auth.module.ts` (o `DbModule` já está importado e fornece o `PrismaService`; `JwtService` e `JwtGuard` vêm do `SharedModule` global).
- Propagar a flag `admin` pela infra de JWT em `apps/backend/src/shared`: adicionar `admin: boolean` ao `JwtPayload`; criar o tipo `AppUser = AuthenticatedUser & { admin: boolean }` em `src/shared/types/app-user.type.ts` (exportar no `index.ts`); `mapPayloadToAuthenticatedUser`, `AuthenticatedRequest.user` e `@CurrentUser()` passam a usar `AppUser`. Não alterar `AuthenticatedUser` no shared.
- Atualizar o `auth.controller` para receber diretamente no construtor `UserPrisma`, `PasswordPrisma`, `BcryptProvider`, `PrismaService` (como `TransactionManager`) e `JwtService`, sem interfaces. Aplicar `@UseGuards(JwtGuard)` na classe (o guard não é global) e marcar com `@Public()` os métodos abertos. Remover o `GET /auth` de exemplo. (skill: backend-controller)
- Criar em `auth.controller` o método `POST /auth/register`, `@Public()`, que instancia o caso de uso `CreateUser` dentro do método, executa e mapeia falhas: `EMAIL_ALREADY_EXISTS` -> `ConflictException`; demais falhas do `Result` -> `BadRequestException` com os códigos de erro. Sucesso responde `201` sem corpo.
- Criar em `auth.controller` o método `POST /auth/login`, `@Public()`, com body `{ email, password }`, que instancia `AuthenticateUser` dentro do método. Falha `INVALID_CREDENTIALS` -> `UnauthorizedException` (`401`); demais falhas -> `BadRequestException`. Sucesso responde `200` com `{ token, user }`, onde `token` é assinado pelo `JwtService` com payload `{ sub: id, name, email, admin }` e `user` é o `UserDTO`.
- Criar em `auth.controller` o método `GET /auth/me` (protegido pelo JWT) que devolve o `@CurrentUser()` (`AppUser`, com `admin`). Fica disponível para o checkout e para revalidação de sessão em funcionalidades futuras.

- Criar o seed em `apps/backend/prisma/seed/data/users.json` com 80 usuários (id uuid v4, name com nome e sobrenome válidos para `PersonName`, email único, avatarUrl http(s) válida ou null, `admin` boolean, `password: "#Senha123"`). O primeiro tem o email `usuario@formacao.dev` e `admin: true`; o segundo tem o email `admin@jaja.dev` e `admin: true`; todos os demais têm `admin: false`.
- Criar a task `apps/backend/prisma/seed/tasks/auth.seed.ts` que lê o JSON, faz `upsert` por email (incluindo `admin`) e grava a senha com hash bcrypt (hashear `#Senha123` uma única vez e reutilizar). Registrar a task no array `seedTasks` de `apps/backend/prisma/seed/main.ts`.
- Executar `npm run prisma:seed --workspace=@jaja/backend`.
- Criar os testes de integração no padrão Rest Client (VS Code) em `apps/backend/src/modules/auth/test/auth.integration.http` cobrindo: registro com sucesso (201), email duplicado (409), senha fraca (400), nome/email inválidos (400); login do admin do seed (200 com `token` e `user.admin = true`), login de usuário comum (200 com `user.admin = false`), senha errada (401), email inexistente (401); `GET /auth/me` com token (200) e sem token (401). Subir o backend (`npm run dev --workspace=@jaja/backend`, porta 4000) e validar as chamadas.

# Frontend

- Em `apps/frontend/src/modules/auth/data/` (arquivos flat, sem subpastas), atualizando o `data/index.ts`:
  - `auth.types.ts`: `AuthUser` (`{ id, name, email, avatarUrl, admin }`) e `AuthSession` (`{ token, user }`).
  - `auth.api.ts`: `login(email, password)` chamando `POST /auth/login` e `register({ name, email, password })` chamando `POST /auth/register` (201 sem corpo), ambos com `fetch` em `NEXT_PUBLIC_API_URL` e tratando o payload de erro `{ statusCode, message, details }` do backend (`message` é um array de códigos): `INVALID_CREDENTIALS` -> "Email ou senha inválidos", `EMAIL_ALREADY_EXISTS` -> "Este email já está cadastrado".
  - `login.schema.ts` (`Email`, `StrongPassword`) e `register.schema.ts` (`name` `PersonName`, `email` `Email`, `password` `StrongPassword`, `confirmPassword` igual à senha via `.refine`, mensagem "As senhas não conferem") usando o validador `v` de `@/shared/components/form/validator`. (skill: frontend-form-schema)
    > Os componentes `Form/FormField/FormMessage/FormButtonSubmit` citados na skill NÃO existem neste projeto. Usar `react-hook-form` com `Input`, `Label`, `Button`, `Card` e `FormErrorMessage` de `@/shared/components/ui`.
  - `auth-storage.ts`: lê/grava/remove a sessão em um cookie `jaja_auth` (JSON de `AuthSession`, `SameSite=Lax`, `path=/`, 7 dias), com leitura **síncrona** de `document.cookie` (guardada contra SSR).
  - `auth.context.tsx`: `AuthProvider` + hook `useAuth` expondo `session`, `user`, `isAuthenticated`, `isAdmin`, `signIn(email, password)`, `signUp({ name, email, password })` (= `register` + `signIn`, devolve o usuário) e `signOut()`. O estado inicial é lido do cookie via lazy initializer do `useState` (`useState(readAuthSession)`) — **nunca inicializar a partir do cookie dentro de `useEffect`**, para não haver um render intermediário sem sessão que faça o guard redirecionar no reload.
- Criar em `apps/frontend/src/modules/auth/components`:
  - `login-form.component.tsx`: formulário de login (email, senha) com todas as validações do schema, estado de carregamento sem spinner (texto/réguas em `#B0ADA4`), erro da API em `FormErrorMessage`. Recebe `onSuccess(user)` (e `defaultEmail?`) e **não** decide redirecionamento nem checa `admin`: quem decide é a página.
  - `register-form.component.tsx`: nome, email, senha e confirmação com o `register.schema`; chama `signUp` e devolve o usuário em `onSuccess(user)`; erro da API em `FormErrorMessage`.
  - `auth-form.component.tsx`: combina os dois em `Tabs` "Entrar" / "Criar conta" (aba ativa com sublinhado 2px vermelho, conforme o design), com `defaultMode?` e `onSignedIn(user, mode)`. O registro sempre autentica em seguida, então as duas abas terminam no mesmo callback.
  - `admin-guard.component.tsx`: protege a área administrativa via `useAuth`. Sem sessão -> `router.replace('/admin/login')`; sessão de usuário não-admin -> `signOut()` e `replace('/admin/login')`; caso contrário renderiza `children`. Enquanto redireciona, não renderiza nada (sem tela de carregamento animada).
- Criar `apps/frontend/src/modules/auth/pages/login.page.tsx` (exportar em `modules/auth/index.ts`): renderiza o `AuthForm` centralizado sobre o papel, com o wordmark "já já." e o rótulo "área administrativa" em caixa alta 11px, e aplica a regra administrativa no `onSignedIn`: `user.admin === true` -> `toast.success` + `router.replace('/admin')`; `false` -> `signOut()` + `toast.error` ("Acesso restrito a administradores" no login; "Conta criada, mas o acesso à área administrativa é restrito a administradores" no registro), permanecendo em `/admin/login`.
- Acesso do cliente na loja:
  - Criar `src/shared/navigation/storefront-routes.ts` (`STOREFRONT_ROUTE = '/'`, `STOREFRONT_LOGIN_ROUTE = '/entrar'`).
  - Criar `apps/frontend/src/modules/auth/pages/storefront-login.page.tsx` e a rota pública `src/app/(public)/entrar/page.tsx` (dentro do `StorefrontShell`, como toda rota pública): título display "Entre ou crie sua conta." com o ponto em vermelho e o `AuthForm` em um `Card` até `max-w-md`. Lê `voltar` com `useSearchParams` (em `Suspense`), aceita apenas caminhos iniciados por `/` (padrão `/`); sessão já existente -> `router.replace(voltar)`; no `onSignedIn` -> `toast.success("Bem-vindo, <primeiro nome>")` ou `"Conta criada. Bem-vindo, <primeiro nome>"` + `router.replace(voltar)`.
  - `StorefrontHeader` (`@/shared/components/store`) ganha as props opcionais `userName?: string | null`, `signInHref?: string` e `onSignOut?: () => void`, renderizadas entre o ETA e a sacola: com `userName`, "olá, <primeiro nome>" em `--muted-ink` e o botão de texto "sair" (hover vermelho); sem `userName` e com `signInHref`, o link "entrar" sublinhado; sem nenhum dos dois, nada. O componente continua controlado por props, sem `useAuth`.
  - `StorefrontShell` (`modules/catalog/components`) alimenta o cabeçalho com `useAuth`: `userName = user?.name`, `signInHref = /entrar?voltar=<pathname + query atual>` (na própria `/entrar`, o link aponta para a URL atual, sem aninhar `voltar`), `onSignOut` = `signOut()` + `toast.success("Até já já.")` sem trocar de rota. O skeleton do cabeçalho ganha o placeholder "entrar". Nenhum link para `/admin` na loja.
- Criar o módulo frontend `apps/frontend/src/modules/admin/` (`components/admin-dashboard.component.tsx`, `pages/admin-dashboard.page.tsx`, `data/dashboard.mock.ts`, `data/index.ts`, `index.ts`) com o dashboard da área administrativa — ele não pertence ao módulo `auth`. Conteúdo: saudação com o nome do usuário logado (`useAuth`), quatro `MetricCard` (pedidos hoje, faturamento do dia, ticket médio, produtos ativos) e um `DashboardRankingListCard` com os produtos mais vendidos, tudo com **dados mock** (sem API) e preços via `formatPrice`. Usar os componentes de `@/shared/components/ui` já restilizados no design flat.
- Reorganizar as rotas do App Router para que **tudo administrativo fique sob `/admin`**:
  - Criar `src/app/admin/login/page.tsx` (público, **sem** `AdminShell` e sem guard) renderizando `LoginPage`.
  - Mover `src/app/(private)/*` para `src/app/admin/(shell)/*`: o `layout.tsx` do grupo passa a usar `AdminGuard` (no lugar do `RouteGuard`) + `ShellProvider` + `AdminShell` + `AppSidebarNavigation`; `principal/page.tsx` vira `src/app/admin/(shell)/page.tsx` renderizando `AdminDashboardPage`; `catalog`, `orders`, `customers`, `stores` viram `/admin/catalog`, `/admin/orders`, `/admin/customers`, `/admin/stores`. Remover `auth/dashboard` (e `modules/auth/pages/dashboard.page.tsx` + `components/auth-dashboard.component.tsx`).
    > O grupo `(shell)` existe para que `/admin/login` não herde o layout com guard e sidebar.
  - Remover `src/app/(public)/auth/page.tsx` e a exceção `/auth` em `src/app/(public)/layout.tsx` (todas as rotas públicas usam o `StorefrontShell`).
  - Remover `src/shared/auth/route-guard.component.tsx` (e a pasta `src/shared/auth`, se ficar vazia).
- Navegação (`src/shared/navigation`): substituir `principal-routes.ts` e `auth-routes.ts` por `admin-routes.ts` (`ADMIN_ROUTE = '/admin'`, `ADMIN_LOGIN_ROUTE = '/admin/login'`); prefixar `CATALOG_ROUTE`, `ORDERS_ROUTE`, `CUSTOMERS_ROUTE` e `STORES_ROUTE` com `/admin`. Em `app-modules.ts`: o módulo `principal` vira `dashboard` ("Visão geral", `ADMIN_ROUTE`, ícone `LayoutDashboard`), remover o módulo `auth` do rail, e em `resolveAppSidebarState` o item `/admin` só casa por igualdade exata (senão `startsWith('/admin/')` captura todas as rotas).
- `AdminShell` na área administrativa: alimentar `userName`, `userEmail` e `userAvatarUrl` a partir de `useAuth().user`, `logoHref = ADMIN_ROUTE`, `onLogout` chama `signOut()` e redireciona para `/admin/login`; ocultar o item "Perfil" do menu do usuário (não há tela de perfil) — manter a prop `profileHref` opcional no componente.
- Montar `<AuthProvider>` e o `<Toaster />` de `@/shared/components/ui/toaster` no `apps/frontend/src/app/layout.tsx` (hoje nenhum dos dois está montado). Na loja, só o `StorefrontShell` consome `useAuth`; a vitrine em si (componente, hook e mock) não muda.
- Validação: `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend` sem erros; com backend e banco no ar, subir `npm run dev --workspace=@jaja/frontend` e conferir: `/` continua pública e sem login; `/admin` sem sessão redireciona para `/admin/login`; login com `usuario@formacao.dev` / `#Senha123` leva ao dashboard em `/admin` com nome e email no menu; reload em `/admin/catalog` mantém a sessão; login com um usuário `admin: false` do seed exibe o toast de acesso restrito e permanece em `/admin/login`; "Criar conta" em `/admin/login` cria o usuário com `admin = false`, exibe a mensagem de acesso restrito e não guarda sessão; "Sair" volta para `/admin/login`. Na loja: `/` sem sessão mostra "entrar" no cabeçalho; "entrar" leva a `/entrar?voltar=...`; criar conta ou entrar volta à página de origem com "olá, Nome" e "sair"; `voltar=https://...` é ignorado (volta para `/`); `/entrar` já logado volta direto; "sair" mantém a rota e mostra "Até já já."; email repetido mostra "Este email já está cadastrado" nas duas telas.

> Obs: IMPORTANTE!!! Executar as três partes (Negócio, Backend e Frontend) em subagentes separados com contexto limpo em cada um deles, de forma sequencial (Backend e Frontend dependem do build de `@jaja/auth`; o Frontend depende do backend no ar para a validação final). Cada subagente do Frontend deve ler `apps/frontend/DESIGN.md` antes de escrever qualquer arquivo.
