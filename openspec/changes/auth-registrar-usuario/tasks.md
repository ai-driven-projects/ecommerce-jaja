## 1. Domínio `@jaja/auth`: limpeza e agregados

- [x] 1.1 Remover `modules/auth/src/auth`, `modules/auth/test/auth` e `modules/auth/test/mock/in-memory-auth.repository.ts` e limpar `modules/auth/src/index.ts`. Verificar que `npm run build --workspace=@jaja/auth` compila com o índice vazio e que `grep -rn "CreateAuth\|AuthDTO" modules/auth/src modules/auth/test` retorna vazio.
- [x] 1.2 Rodar `node .claude/skills/module-aggregate/scripts/create-aggregate.js --module auth --aggregate user --mode example` e `... --aggregate password --mode example`, apagar os use cases e testes de exemplo gerados e manter `model/`, `provider/`, `dto/` e os mocks `test/mock/in-memory-user.repository.ts` e `in-memory-password.repository.ts`. Verificar com `find modules/auth/src -type f` que não sobrou nenhum `*.use-case.ts` de exemplo.
- [x] 1.3 Implementar `modules/auth/src/user/model/user.entity.ts` (`UserProps { id?, name, email, avatarUrl?, admin }`, `tryCreate` com `Result.combine` de `Id`, `PersonName`, `Email`, `Url` opcional e `Flag`, `create`, getters `name`/`email`/`avatarUrl`/`isAdmin`) e `modules/auth/src/user/dto/user.dto.ts` (`UserDTO { id, name, email, avatarUrl, admin }` + `toDTO` na entidade). Verificar com testes em `modules/auth/test/user/user.entity.test.ts`: cria com `admin` omitido → `false`, `admin: 'yes'` falha, nome sem sobrenome falha, `avatarUrl` inválida falha, `toDTO` não expõe nada além dos cinco campos.
- [x] 1.4 Implementar `modules/auth/src/user/provider/user.repository.ts` (`UserRepository extends CrudRepository<User>` + `findByEmail(email): Promise<Result<User | null>>`) e o mock in-memory correspondente. Verificar com teste do mock que `findByEmail` de email ausente retorna `Result.ok(null)`.
- [x] 1.5 Implementar `modules/auth/src/password/model/password.entity.ts` (`PasswordProps { id?, userId, value }`, `userId` via `Id`, `value` via `Password` do shared) e testes em `modules/auth/test/password/password.entity.test.ts`. Verificar que um hash bcrypt válido passa e que `"#Senha123"` em texto puro falha.
- [x] 1.6 Implementar `modules/auth/src/password/provider/password.repository.ts` (`create(password, tx?)`, `findByUserId(userId)`), `password-crypto.provider.ts` (`hash`, `compare`) e o mock in-memory. Verificar que `findByUserId` de usuário sem senha retorna `Result.ok(null)`.

## 2. Domínio `@jaja/auth`: casos de uso e testes

- [x] 2.1 Implementar `modules/auth/src/user/use-case/create-user.use-case.ts` (`CreateUser implements UseCase<CreateUserInput, void>`; construtor `UserRepository`, `PasswordRepository`, `PasswordCryptoProvider`, `TransactionManager`; fluxo: `findByEmail` → `EMAIL_ALREADY_EXISTS`, `PersonName`, `Email`, `StrongPassword`, `hash`, `User.tryCreate` com `admin: false`, `Password.tryCreate`, `runInTransaction` repassando `tx` aos dois `create`). Verificar com `modules/auth/test/user/create-user.use-case.test.ts` (mocks in-memory, provider fake com hash bcrypt fixo, `TransactionManager` fake que passa um contexto): sucesso grava usuário com `admin = false` e senha com o hash; email duplicado, senha fraca e nome inválido falham com os códigos esperados sem gravar nada; entrada com `admin: true` extra continua gravando `admin = false`.
- [x] 2.2 Implementar `modules/auth/src/user/use-case/authenticate-user.use-case.ts` (`AuthenticateUser implements UseCase<{ email, password }, UserDTO>`; construtor `UserRepository`, `PasswordRepository`, `PasswordCryptoProvider`; usuário ausente, senha ausente ou `compare` falso → `Result.fail('INVALID_CREDENTIALS')`; sucesso → `Result.ok(user.toDTO())`). Verificar com `modules/auth/test/user/authenticate-user.use-case.test.ts`: sucesso retorna `admin` correto e nenhum campo de senha; email inexistente e senha errada retornam exatamente o mesmo array de erros `['INVALID_CREDENTIALS']`.
- [x] 2.3 Exportar entidades, DTO, repositórios, provider e casos de uso em `modules/auth/src/{user,password}/index.ts` e `modules/auth/src/index.ts`. Verificar que `npm test --workspace=@jaja/auth` passa e que `npm run build --workspace=@jaja/auth` gera `dist/index.d.ts` contendo `CreateUser`, `AuthenticateUser`, `UserDTO`, `UserRepository`, `PasswordRepository` e `PasswordCryptoProvider`.

## 3. Backend: banco, adapters e tipos de JWT

- [x] 3.1 Conferir que `apps/backend/docker-compose.yml`, `apps/backend/.env` e `.env.example` usam a porta 5433, definir um `JWT_SECRET` real no `.env`, subir o banco com `npm run db:start --workspace=@jaja/backend` e instalar `bcrypt` + `@types/bcrypt` em `apps/backend`. Verificar que `docker compose ps` mostra o Postgres saudável na 5433 e que `bcrypt` aparece em `apps/backend/package.json`.
- [x] 3.2 Mapear em `apps/backend/prisma/models/auth.model.prisma` os modelos `User` (`@@map("users")`, `email @unique`, `avatarUrl String? @map("avatar_url")`, `admin Boolean @default(false)`, timestamps, relação `password Password?`) e `Password` (`@@map("passwords")`, `userId @unique @map("user_id")`, `value`, FK `onDelete: Cascade`). Rodar `npm run prisma:migrate:dev --workspace=@jaja/backend -- --name auth_user_password` e `npm run prisma:generate --workspace=@jaja/backend`. Verificar que a migration criada contém as duas tabelas, o índice único de email e `ON DELETE CASCADE`.
- [x] 3.3 Implementar `apps/backend/src/modules/auth/user.prisma.ts` (`UserPrisma implements UserRepository`, `toDomain`/`fromDomain` com `admin`, `create/update/findById/findAll/delete/findByEmail` retornando `Result`, cliente `(tx as PrismaTransactionContext)?.client ?? this.prisma.client`) e `password.prisma.ts` (`PasswordPrisma implements PasswordRepository`, mesmo padrão). Verificar que `npm run build --workspace=@jaja/backend` compila e que `findByEmail` de email inexistente retorna `Result.ok(null)` (teste manual via endpoint na tarefa 4.4).
- [x] 3.4 Implementar `apps/backend/src/modules/auth/bcrypt.provider.ts` (`BcryptProvider implements PasswordCryptoProvider`, salt rounds 10), remover `auth.prisma.ts` e registrar `UserPrisma`, `PasswordPrisma` e `BcryptProvider` em `auth.module.ts`. Verificar que `npm run build --workspace=@jaja/backend` compila e que `auth.prisma.ts` não existe mais.
- [x] 3.5 Propagar `admin` na infra de JWT: `admin: boolean` em `src/shared/types/jwt-payload.type.ts`; novo `src/shared/types/app-user.type.ts` (`AppUser = AuthenticatedUser & { admin: boolean }`, exportado em `types/index.ts`); `auth-user.mapper.ts` retorna `AppUser` com `admin: payload.admin`; `authenticated-request.type.ts` e `current-user.decorator.ts` usam `AppUser`. Verificar que `git diff --stat packages/shared` está vazio e que o backend compila.

## 4. Backend: endpoints, seed e testes de integração

- [x] 4.1 Reescrever `apps/backend/src/modules/auth/auth.controller.ts`: construtor com `UserPrisma`, `PasswordPrisma`, `BcryptProvider`, `PrismaService`, `JwtService`; `@UseGuards(JwtGuard)` na classe; remover `GET /auth`; `POST /auth/register` `@Public()` + `@HttpCode(201)` instanciando `CreateUser` e mapeando `EMAIL_ALREADY_EXISTS` → `ConflictException` e demais erros → `BadRequestException` com os códigos. Verificar com `curl` que um registro válido responde 201 sem corpo e o mesmo email repetido responde 409.
- [x] 4.2 Adicionar `POST /auth/login` `@Public()` (instancia `AuthenticateUser`; `INVALID_CREDENTIALS` → `UnauthorizedException`; demais → `BadRequestException`; sucesso `200` com `{ token: jwtService.sign({ sub, name, email, admin }), user }`) e `GET /auth/me` (retorna `@CurrentUser()`). Verificar com `curl` que o login do usuário registrado em 4.1 devolve token e `user.admin = false`, que senha errada devolve 401 e que `GET /auth/me` com o token devolve 200 e sem token devolve 401.
- [x] 4.3 Criar `apps/backend/prisma/seed/data/users.json` com 80 usuários (`id` uuid v4, `name` com nome e sobrenome, `email` único, `avatarUrl` http(s) ou `null`, `admin`, `password: "#Senha123"`), sendo o primeiro `usuario@formacao.dev` (`admin: true`), o segundo `admin@jaja.dev` (`admin: true`) e os demais `admin: false`. Verificar com um script rápido em node que há 80 entradas, emails únicos, todo nome tem ao menos duas palavras e exatamente dois `admin: true`.
- [x] 4.4 Criar `apps/backend/prisma/seed/tasks/auth.seed.ts` (lê o JSON, `bcrypt.hash('#Senha123', 10)` uma vez, `upsert` por email gravando `admin` e a senha) e registrá-la em `seedTasks` de `prisma/seed/main.ts`. Rodar `npm run prisma:seed --workspace=@jaja/backend` duas vezes. Verificar via `prisma studio` ou SQL que existem exatamente 80 usuários e 80 senhas e que os dois administradores têm `admin = true`.
- [x] 4.5 Criar `apps/backend/src/modules/auth/test/auth.integration.http` cobrindo: registro 201, email duplicado 409, senha fraca 400, nome/email inválidos 400; login do admin do seed 200 com `user.admin = true`, login de usuário comum 200 com `user.admin = false`, senha errada 401, email inexistente 401; `GET /auth/me` com token 200 e sem token 401. Verificar executando todas as requisições com o backend no ar (`npm run dev --workspace=@jaja/backend`) e conferindo os status esperados.

## 5. Frontend: sessão e módulo `auth`

- [x] 5.1 Criar em `apps/frontend/src/modules/auth/data/`: `auth.types.ts` (`AuthUser`, `AuthSession`), `auth.api.ts` (`login(email, password)` com `fetch` em `NEXT_PUBLIC_API_URL`, lançando erro com a `message`/`details` do payload `{ statusCode, message, details }`), `login.schema.ts` (`v` com `Email` e `StrongPassword`) e `auth-storage.ts` (cookie `jaja_auth`, JSON, `path=/`, `SameSite=Lax`, 7 dias; `readAuthSession` síncrono e seguro em SSR; `writeAuthSession`; `clearAuthSession`). Atualizar `data/index.ts`. Verificar que `npm run lint --workspace=@jaja/frontend` passa e que, no console do navegador, `document.cookie` mostra `jaja_auth` após um `writeAuthSession` de teste.
- [x] 5.2 Criar `auth.context.tsx` (`AuthProvider` com `useState(readAuthSession)`, `useAuth` expondo `session`, `user`, `isAuthenticated`, `isAdmin`, `signIn`, `signOut`; sem `useEffect` para inicializar) e montar `<AuthProvider>` e `<Toaster />` em `apps/frontend/src/app/layout.tsx`. Verificar que `grep -n "useEffect" auth.context.tsx` não aparece na inicialização e que a vitrine `/` continua renderizando normalmente.
- [x] 5.3 Criar `components/login-form.component.tsx` (`react-hook-form` + schema; campos email e senha com `Input`/`Label`; erro da API em `FormErrorMessage`; estado de envio com texto em `#B0ADA4`, sem spinner; sucesso não-admin → `signOut()` + `toast.error("Acesso restrito a administradores")`; sucesso admin → `toast.success` + `router.replace('/admin')`) e `pages/login.page.tsx` (wordmark "já já.", rótulo "área administrativa" em caixa alta 11px, formulário centralizado sobre papel). Exportar em `modules/auth/index.ts` e remover `pages/dashboard.page.tsx` e `components/auth-dashboard.component.tsx`. Verificar visualmente conforme `DESIGN.md` (sem radius, sem sombra) e que submeter `x@` / `123` mostra erros sem chamada de rede.
- [x] 5.4 Criar `components/admin-guard.component.tsx` (`useAuth`; sem sessão → `router.replace('/admin/login')`; sessão com `admin = false` → `signOut()` + `replace`; senão renderiza `children`; retorna `null` enquanto redireciona). Verificar depois da tarefa 6.2 que `/admin` sem cookie redireciona e que um cookie manipulado com `admin: false` é apagado e redirecionado.

## 6. Frontend: rotas `/admin`, navegação, shell e dashboard

- [x] 6.1 Criar `apps/frontend/src/modules/admin/` (`data/dashboard.mock.ts` com os quatro indicadores e o ranking derivado de `PRODUCTS` do catálogo, `data/index.ts`, `components/admin-dashboard.component.tsx` com saudação `useAuth().user`, quatro `MetricCard` e `DashboardRankingListCard` com props fixas, `pages/admin-dashboard.page.tsx`, `index.ts`). Verificar que o componente não faz `fetch` e que os valores monetários usam `formatPrice`.
- [x] 6.2 Reorganizar as rotas: criar `src/app/admin/login/page.tsx` (renderiza `LoginPage`, sem shell) e `src/app/admin/(shell)/layout.tsx` (`AdminGuard` + `ShellProvider` + `AdminShell` + `AppSidebarNavigation`); mover as páginas de `src/app/(private)` para `src/app/admin/(shell)/{page.tsx,catalog,orders,customers,stores}` (`page.tsx` renderiza `AdminDashboardPage`); remover `src/app/(private)`, `src/app/(public)/auth/page.tsx`, a exceção `/auth` em `src/app/(public)/layout.tsx` e `src/shared/auth/route-guard.component.tsx` (e a pasta se vazia). Verificar que `/principal` e `/auth` respondem 404, `/admin/login` abre sem sidebar e `/admin` sem sessão redireciona para `/admin/login`.
- [x] 6.3 Atualizar `src/shared/navigation`: criar `admin-routes.ts` (`ADMIN_ROUTE`, `ADMIN_LOGIN_ROUTE`), remover `principal-routes.ts` e `auth-routes.ts`, prefixar `CATALOG_ROUTE`, `ORDERS_ROUTE`, `CUSTOMERS_ROUTE`, `STORES_ROUTE` com `/admin`; em `app-modules.ts` trocar `principal` por `dashboard` ("Visão geral", `ADMIN_ROUTE`), remover o módulo `auth` e fazer `resolveAppSidebarState` casar o dashboard só por igualdade exata. Verificar que em `/admin/catalog` o item ativo é Catálogo (não Visão geral) e que `grep -rn "'/principal'\|'/catalog'\|'/orders'\|'/customers'\|'/stores'\|'/auth'" apps/frontend/src` retorna vazio.
- [x] 6.4 Ligar o `AdminShell` à sessão no layout `(shell)`: `userName`, `userEmail`, `userAvatarUrl` de `useAuth().user`, `logoHref = ADMIN_ROUTE`, `onLogout` = `signOut()` + `router.replace(ADMIN_LOGIN_ROUTE)`; ocultar o item "Perfil" do menu quando `profileHref` não for informado (prop continua opcional). Verificar que o menu mostra nome e email do admin logado, não mostra "Perfil", e que "Sair" leva a `/admin/login` e apaga o cookie `jaja_auth`.

## 7. Validação final

- [x] 7.1 Rodar `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend`. Verificar que ambos terminam sem erros e sem aviso de hooks de cliente fora de `Suspense`.
- [x] 7.2 Com banco, backend (`npm run dev --workspace=@jaja/backend`) e frontend (`npm run dev --workspace=@jaja/frontend`) no ar, conferir: `/` pública e sem controle de login; `/admin` sem sessão → `/admin/login`; login `usuario@formacao.dev` / `#Senha123` → dashboard em `/admin` com saudação, quatro indicadores, ranking e nome/email no menu; reload em `/admin/catalog` mantém a sessão sem passar pelo login; login com um usuário `admin: false` do seed exibe o toast de acesso restrito e permanece em `/admin/login`; "Sair" volta para `/admin/login`. Verificar cada item e registrar o resultado.

## 8. Login e registro do cliente na vitrine e registro pelo /admin

- [x] 8.1 Em `apps/frontend/src/modules/auth/data/`: `auth.api.ts` ganha `register({ name, email, password })` (`POST /auth/register`, 201 sem corpo; `EMAIL_ALREADY_EXISTS` → "Este email já está cadastrado"); `register.schema.ts` (`name` `PersonName`, `email` `Email`, `password` `StrongPassword`, `confirmPassword` com `.refine` igual à senha, mensagem "As senhas não conferem"); `auth.context.tsx` ganha `signUp({ name, email, password })` = `register` + `signIn`. Atualizar `data/index.ts`. Verificar com `npm run lint --workspace=@jaja/frontend` e chamando `register` duas vezes com o mesmo email (a segunda lança a mensagem de email cadastrado).
- [x] 8.2 Refatorar `components/login-form.component.tsx` para receber `onSuccess(user)` (e `defaultEmail?`), sem redirecionar nem checar `admin`; criar `components/register-form.component.tsx` (nome, email, senha, confirmação; `signUp`; `onSuccess(user)`; erro da API em `FormErrorMessage`) e `components/auth-form.component.tsx` (`Tabs` "Entrar"/"Criar conta", `defaultMode?`, `onSignedIn(user, mode)`). Exportar em `modules/auth/index.ts`. Verificar que confirmação diferente da senha e nome sem sobrenome são barrados sem chamada de rede.
- [x] 8.3 `pages/login.page.tsx` (`/admin/login`) passa a renderizar `AuthForm` e a decidir: `admin` → `toast.success` + `replace('/admin')`; não-admin → `signOut()` + `toast.error` ("Acesso restrito a administradores" no login, "Conta criada, mas o acesso à área administrativa é restrito a administradores" no registro). Verificar: criar conta em `/admin/login` → usuário existe no banco com `admin = false`, mensagem exibida, cookie vazio, permanece em `/admin/login`; login admin continua levando ao dashboard.
- [x] 8.4 Criar `src/shared/navigation/storefront-routes.ts` (`STOREFRONT_ROUTE = '/'`, `STOREFRONT_LOGIN_ROUTE = '/entrar'`), `pages/storefront-login.page.tsx` (título display "Entre ou crie sua conta." com ponto vermelho, `AuthForm` em `Card` até `max-w-md`, lê `voltar` com `useSearchParams` em `Suspense`, aceita só caminhos iniciados por `/`, padrão `/`; sessão existente → `replace(voltar)`; sucesso → `toast.success` ("Bem-vindo, <nome>" ou "Conta criada. Bem-vindo, <nome>") + `replace(voltar)`) e a rota `src/app/(public)/entrar/page.tsx`. Verificar que `/entrar?voltar=/?bairro=Meireles` volta para a vitrine com o bairro após entrar e que `voltar=https://x` é ignorado.
- [x] 8.5 `StorefrontHeader` ganha `userName?`, `signInHref?`, `onSignOut?` (controle entre o ETA e a sacola: "olá, <primeiro nome>" + "sair", ou link "entrar"); `StorefrontShell` alimenta com `useAuth` (`signInHref = /entrar?voltar=<pathname+query>`; `onSignOut` = `signOut()` + `toast.success("Até já já.")`) e o skeleton do cabeçalho ganha o placeholder "entrar". Verificar que `/` sem sessão mostra "entrar", com sessão mostra "olá, Nome" e "sair", e que "sair" mantém a rota.
- [x] 8.6 Rodar `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend`; com banco, backend e frontend no ar conferir: `/` sem sessão com "entrar"; "entrar" → `/entrar?voltar=...`; criar conta na loja → volta à origem logado com "olá, Nome"; "sair" → "entrar" volta a aparecer; `/entrar` já logado → volta para `voltar`; `/admin/login` com abas, registro de não-admin barrado com a mensagem e login admin → `/admin`; email repetido mostra "Este email já está cadastrado" nas duas telas. Verificar cada item e registrar o resultado.
