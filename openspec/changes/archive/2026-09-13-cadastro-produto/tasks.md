## 1. Pré-requisitos (executar antes de qualquer subagente)

- [x] 1.1 Conferir que as funcionalidades 06, 07 e 08 estão aplicadas: `modules/catalog/src/brand` e `src/category` (entidades, repositórios, `DeleteBrand`, `DeleteCategory`, mocks `test/mock/in-memory-brand.repository.ts` e `in-memory-category.repository.ts`); `AdminOnly` em `apps/backend/src/shared/decorators`; `BrandPrisma`, `CategoryPrisma`, `BrandController` e `CategoryController` em `apps/backend/src/modules/catalog/catalog.module.ts`; models `Brand` e `Category` em `catalog.model.prisma`; `seedCatalogBrands` e `seedCatalogCategories` em `prisma/seed/main.ts`; `apiRequest`/`ApiError` em `apps/frontend/src/shared/util/api-client.util.ts`; `listBrands` e `listCategories` em `apps/frontend/src/modules/catalog/data`; seção `catalog-registrations` com "Marcas" e "Categorias" em `app-modules.ts`. Verificar com `grep -rn` para cada símbolo; se algum faltar, **parar** e aplicar antes `area-admin-base`, o cadastro de marca e o cadastro de categoria.
- [x] 1.2 Conferir os contratos usados por este change: `CategoryDTO.path` com separador ` / `, construtores atuais de `DeleteBrand` e `DeleteCategory`, assinatura de `apiRequest` e caminhos das capabilities de marca e categoria. Verificar com `openspec list --specs` que existem `catalog/brand-registration` e `catalog/category-registration`; se os nomes forem outros, mover `specs/catalog/brand-registration` e `specs/catalog/category-registration` deste change para os caminhos reais e rodar `openspec validate cadastro-produto --strict` sem erros.
- [x] 1.3 Subir o banco (`npm run db:start --workspace=@jaja/backend`, porta 5433), aplicar migrations e seeds existentes. Verificar que `brands` tem 210 registros, que há 12 categorias raiz e que `npm test --workspace=@jaja/catalog` passa antes de qualquer alteração.

## 2. Negócio: scaffold, VOs e entidade (subagente Negócio, lê `.claude/skills/skills-standards.md`)

- [x] 2.1 Rodar `node .claude/skills/module-aggregate/scripts/create-aggregate.js --module catalog --aggregate product --mode example`, apagar o use case e o teste de exemplo gerados e manter `model/`, `provider/`, `dto/` e `test/mock/in-memory-product.repository.ts`. Verificar com `find modules/catalog/src/product modules/catalog/test -name "*product*"` que não sobrou `*.use-case.ts` nem teste de exemplo.
- [x] 2.2 Implementar em `modules/catalog/src/product/model/` os VOs `ProductName` (`product-name.vo.ts`, estende `Text`, trim, 3–255, códigos `PRODUCT_NAME_TOO_SHORT`/`PRODUCT_NAME_TOO_LONG`), `ProductDescription` (`product-description.vo.ts`, opcional, até 5000, `PRODUCT_DESCRIPTION_TOO_LONG`) e `MoneyCents` (`money-cents.vo.ts`, inteiro ≥ 1, `MONEY_CENTS_INVALID`) com a skill `module-value-object`. Verificar com testes em `modules/catalog/test/product/`: nome com 2 e 256 caracteres falha, 3 e 255 passam, `"  Borracha  "` vira `"Borracha"`; descrição ausente é `null` e 5001 caracteres falha; `MoneyCents` rejeita `0`, `-1`, `1.5` e `NaN` e aceita `1`.
- [x] 2.3 Implementar `product-image.vo.ts` (`ProductImage` com `thumbUrl: Url`, `largeUrl: Url`, `order: Order` e `withOrder(n)`). Verificar com testes: URL `ftp://x` ou vazia falha, `order: -1` falha, `withOrder(3)` devolve nova instância com `order` 3.
- [x] 2.4 Implementar `product.entity.ts` com a skill `module-entity`: props `id`, `name`, `slug` (`Alias`, derivado de `Alias.format(name)` quando ausente), `sku` (`Text` opcional ≤ 40), `brandId` (`Id` opcional), `categoryId` (`Id`), `description`, `priceCents`, `listPriceCents` (opcional), `unit` (`Text` ≤ 40, padrão `"unidade"`), `images` (ordenação estável por `order` e reatribuição `0..n-1`), `isActive` (`Flag`, padrão `true`); `Result.combine` incluindo `PRODUCT_LIST_PRICE_NOT_GREATER_THAN_PRICE` e `PRODUCT_IMAGES_LIMIT_EXCEEDED`; `create`/`tryCreate`, getters e `mainImage`. Verificar com `modules/catalog/test/product/product.entity.test.ts`: nome curto, preço zero, `listPriceCents` igual e menor que o preço, 11 imagens, normalização de `order` (5, 2, 9 → B, A, C), `mainImage` nulo sem imagens e igual à de menor `order` com imagens, padrões de `unit`/`isActive`/`slug`.

## 3. Negócio: repositório, DTOs e queries

- [x] 3.1 Implementar `provider/product.repository.ts` com a skill `module-repository` (`ProductRepository extends CrudRepository<Product>` + `findBySlug`, `findBySku`, `existsByBrandId`, `existsByCategoryId`) e completar o mock in-memory (soft delete preenchendo `deletedAt`; buscas e `existsBy*` ignoram excluídos; `findById` falha com `PRODUCT_NOT_FOUND`; `update` substitui a lista de imagens). Verificar com `modules/catalog/test/mock/in-memory-product.repository.test.ts`: `findBySku` de sku ausente retorna `Result.ok(null)`, `existsByBrandId` fica `false` após excluir o único produto da marca.
- [x] 3.2 Criar `dto/product.dto.ts` com a skill `module-dto`: `ProductImageDTO`, `ProductDTO` (atributos + `images`, `brandName: string | null`, `categoryPath`, `createdAt`, `updatedAt`), `ProductListItemDTO` e `ProductPageDTO`. Verificar que `npm run build --workspace=@jaja/catalog` compila.
- [x] 3.3 Criar com a skill `module-query-cqrs` `provider/find-products.query.ts` (`FindProductsQuery`, filtro `{ page, pageSize, search?, brandId?, categoryId?, isActive? }` → `Result<ProductPageDTO>`) e `provider/find-product-by-id.query.ts` (`FindProductByIdQuery` → `Result<ProductDTO | null>`), documentando no JSDoc: ordenação por nome, `search` em `name`/`slug`/`sku`, `categoryId` com descendentes. Verificar que o build compila.

## 4. Negócio: casos de uso e regras de marca/categoria

- [x] 4.1 Implementar `use-case/save-product.use-case.ts` com a skill `module-use-case` (`SaveProduct`, construtor `ProductRepository`, `BrandRepository`, `CategoryRepository`; retorna `Result<Product>`): marca quando houver `brandId` (`BRAND_NOT_FOUND`), categoria (`CATEGORY_NOT_FOUND`), slug derivado e único (`PRODUCT_SLUG_ALREADY_EXISTS`, ignorando o próprio id), sku único quando houver (`PRODUCT_SKU_ALREADY_EXISTS`), criação com uuid v4 e padrões quando não houver `id` ou não existir, alteração via `cloneWith` substituindo as imagens. Verificar com `modules/catalog/test/product/save-product.use-case.test.ts`: criação com padrões, marca inexistente, categoria inexistente, slug duplicado, sku duplicado, alteração mantendo o próprio slug/sku, alteração substituindo imagens (A, B, C → C, D), `id` informado inexistente cria com esse id.
- [x] 4.2 Implementar `use-case/delete-product.use-case.ts` (`DeleteProduct`, construtor `ProductRepository`; `findById` → `delete`). Verificar com teste: exclusão preenche `deletedAt` e `findById` passa a falhar com `PRODUCT_NOT_FOUND`; id inexistente falha com `PRODUCT_NOT_FOUND`.
- [x] 4.3 Alterar `DeleteBrand` para receber `ProductRepository` e falhar com `BRAND_HAS_PRODUCTS`, e `DeleteCategory` para receber `ProductRepository` e falhar com `CATEGORY_HAS_PRODUCTS` após a verificação de filhas; atualizar os testes existentes para o novo construtor. Verificar com testes: marca com produto é rejeitada e sem produto (ou só com excluídos) é excluída; categoria folha com produto é rejeitada; categoria com filhas e produtos continua falhando com `CATEGORY_HAS_CHILDREN`.
- [x] 4.4 Exportar o agregado em `src/product/index.ts` e `src/index.ts`. Verificar que `npm test --workspace=@jaja/catalog` passa integralmente e que `npm run build --workspace=@jaja/catalog` gera `dist/index.d.ts` contendo `Product`, `SaveProduct`, `DeleteProduct`, `ProductRepository`, `FindProductsQuery`, `FindProductByIdQuery` e `ProductPageDTO`.

## 5. Backend: Prisma (subagente Backend, lê `.claude/skills/skills-standards.md`)

- [x] 5.1 Mapear em `apps/backend/prisma/models/catalog.model.prisma`, com a skill `backend-prisma-data`, os models `Product` (`@@map("products")`, `slug @unique`, `sku String? @unique`, `priceCents Int`, `listPriceCents Int?`, `unit`, `description String?`, `isActive`, `brandId String?` com relação `onDelete: Restrict`, `categoryId String` com relação `onDelete: Restrict`, `@@index([brandId])`, `@@index([categoryId])`, timestamps e `deletedAt DateTime?`, colunas em snake_case) e `ProductImage` (`@@map("product_images")`, `productId` com `onDelete: Cascade`, `thumbUrl`, `largeUrl`, `order`, `@@unique([productId, order])`), e as relações inversas `products Product[]` em `Brand` e `Category`. Rodar `npm run prisma:migrate:dev --workspace=@jaja/backend -- --name catalog_product` e `npm run prisma:generate --workspace=@jaja/backend`. Verificar que o SQL da migration contém as duas tabelas, os índices únicos de `slug`, `sku` e `(product_id, order)`, `ON DELETE RESTRICT` para marca/categoria e `ON DELETE CASCADE` para imagens.

## 6. Backend: adapter e controllers

- [x] 6.1 Criar `apps/backend/src/modules/catalog/product.prisma.ts` (`ProductPrisma implements ProductRepository`) no padrão de `BrandPrisma`: `toDomain`/`fromDomain` com imagens, cliente `(tx as PrismaTransactionContext | undefined)?.client ?? this.prisma.client`, `create` com nested write, `update` com `deleteMany` + `createMany` das imagens na mesma transação, soft delete, `deletedAt: null` em todas as buscas e em `existsBy*`, e tradução de `P2002` por campo (`slug`/`sku`/`id` → `PRODUCT_SLUG_ALREADY_EXISTS`/`PRODUCT_SKU_ALREADY_EXISTS`/`PRODUCT_NOT_FOUND`). Registrar em `providers` e `exports` de `catalog.module.ts`. Verificar que `npm run build --workspace=@jaja/backend` compila.
- [x] 6.2 Implementar em `ProductPrisma` os atributos públicos `findProducts: FindProductsQuery` (mapa de categorias carregado uma vez, ids descendentes por BFS, `where` com `OR` insensitivo, `orderBy` nome + id, `skip`/`take`, `include` de marca e da primeira imagem, `count` no mesmo `$transaction`, `categoryPath` com ` / `) e `findProductById: FindProductByIdQuery` (imagens ordenadas, `categoryPath` pela cadeia de pais, `null` para excluído). Verificar na tarefa 8.1 os totais e caminhos retornados.
- [x] 6.3 Criar `product.controller.ts` com a skill `backend-controller`, `@AdminOnly()` na classe e registrado em `catalog.module.ts`: `POST /products` (201) e `PUT /products/:id` (200) instanciam `SaveProduct` com `ProductPrisma`, `BrandPrisma`, `CategoryPrisma` e respondem com `findProductById` do produto salvo; `GET /products` normaliza `page` (padrão 1), `pageSize` (padrão 20, máximo 100) e `isActive` (`"true"`/`"false"`) antes de chamar `findProducts`; `GET /products/:id` responde 404 quando `null`; `DELETE /products/:id` instancia `DeleteProduct` e responde 204. Mapear `PRODUCT_NOT_FOUND`, `BRAND_NOT_FOUND`, `CATEGORY_NOT_FOUND` → `NotFoundException`; `PRODUCT_SLUG_ALREADY_EXISTS`, `PRODUCT_SKU_ALREADY_EXISTS` → `ConflictException`; demais → `BadRequestException` com os códigos. Verificar com `curl` autenticado que `POST` válido responde 201 e `GET /products?pageSize=500` devolve `pageSize: 100`.
- [x] 6.4 Atualizar `brand.controller.ts` e `category.controller.ts` para instanciar `DeleteBrand`/`DeleteCategory` com `ProductPrisma` e mapear `BRAND_HAS_PRODUCTS`/`CATEGORY_HAS_PRODUCTS` → `ConflictException`. Verificar com `curl` após o seed (tarefa 7.2) que `DELETE /brands/<marca com produtos>` responde 409 com `BRAND_HAS_PRODUCTS`.

## 7. Backend: seed de produtos

- [x] 7.1 Criar `apps/backend/prisma/seed/tasks/catalog-products.seed.ts` (`seedCatalogProducts`) conforme design (decisão 10): leitura de `apps/cli/data/kalunga/categories/*.json` relativa a `import.meta.url` com erro claro se faltar, ordem alfabética de arquivo, deduplicação por `id`, mapas de marcas por slug e de categorias por nomes, slug com sufixo `-<sku>` em colisão, categoria com fallback para o departamento e `console.warn`, descrição truncada em 5000, preços em centavos com `listPriceCents` só quando `list > current`, unidade por regex, até 10 imagens, `isActive = available`, validação com `Product.tryCreate` e `upsert` por `sku` substituindo imagens. Registrar em `seedTasks` de `prisma/seed/main.ts` após `seedCatalogCategories`. Verificar que `npm run build --workspace=@jaja/backend` (ou o `tsc` do seed) compila.
- [x] 7.2 Rodar `npm run prisma:seed --workspace=@jaja/backend` duas vezes. Verificar por SQL: `count(*) = 1076` em `products` nas duas execuções; nenhum produto sem imagem e nenhum com mais de 10; `category_id` nunca nulo; `027242` com slug terminado em `-027242` e `026106` sem sufixo; produtos das marcas `dover` e `dvt-comercio-importa` com `brand_id` nulo; 96 produtos com `list_price_cents` não nulo; 19 produtos com `is_active = false`; contagem de `product_images` igual nas duas execuções.

## 8. Backend: testes de integração

- [x] 8.1 Criar `apps/backend/src/modules/catalog/test/product.integration.http`, obtendo os tokens como em `brand.integration.http`, cobrindo: 401, 403, criação com marca e categoria (201), criação sem marca (201), marca inexistente (404), categoria inexistente (404), preço zero (400), `listPriceCents` menor que o preço (400), 11 imagens (400), slug duplicado (409), sku duplicado (409), listagem paginada com `search`, `brandId` e `categoryId` de departamento (200, conferindo `total` e `totalPages`), busca por id (200/404), alteração substituindo imagens (200), exclusão (204, seguida de 404), `PUT` no id excluído (404) e, por fim, `DELETE /brands/:id` e `DELETE /categories/:id` de registros com produtos (409). Verificar executando todas as requisições com o backend no ar (`npm run dev --workspace=@jaja/backend`, porta 4000) e conferindo os status esperados.

## 9. Frontend: mensagens e camada de dados (subagente Frontend, lê `.claude/skills/skills-standards.md` e `apps/frontend/DESIGN.md`)

- [x] 9.1 Adicionar em `src/shared/i18n/messages.pt.ts` e `messages.en.ts` as mensagens de `PRODUCT_NOT_FOUND`, `PRODUCT_SLUG_ALREADY_EXISTS`, `PRODUCT_SKU_ALREADY_EXISTS`, `BRAND_HAS_PRODUCTS` ("Marca possui produtos cadastrados"), `CATEGORY_HAS_PRODUCTS` ("Categoria possui produtos cadastrados"), `PRODUCT_LIST_PRICE_NOT_GREATER_THAN_PRICE`, `PRODUCT_IMAGES_LIMIT_EXCEEDED`, `MONEY_CENTS_INVALID`, `PRODUCT_NAME_TOO_SHORT`, `PRODUCT_NAME_TOO_LONG` e `PRODUCT_DESCRIPTION_TOO_LONG`. Verificar que `npm run lint --workspace=@jaja/frontend` passa e que as duas tabelas têm as mesmas chaves.
- [x] 9.2 Criar `src/modules/catalog/data/product.api.ts` (`listProducts(token, filter)` montando a query string sem valores vazios, `getProduct`, `createProduct`, `updateProduct`, `deleteProduct` sobre `apiRequest`) e os tipos espelhando os DTOs; atualizar `data/index.ts`. Verificar que o lint passa e que os tipos compilam no build da tarefa 12.4.
- [x] 9.3 Criar `product.schema.ts` com a skill `frontend-form-schema`: `name` (3–255), `slug` (`Alias`), `sku` opcional (≤ 40), `brandId` opcional, `categoryId` obrigatório, `description` opcional (≤ 5000), `price` (> 0, duas casas), `listPrice` opcional com refinamento `> price`, `unit` (≤ 40), `images` com `v.defineArray` de `{ thumbUrl, largeUrl }` (URLs válidas, máximo 10) e `isActive`; exportar `ProductFormData`. Verificar com `v.resolver` em um teste rápido ou no navegador (tarefa 12.5) que `listPrice` 9,00 com `price` 10,00 gera erro no campo `listPrice`.
- [x] 9.4 Criar `use-products.hook.ts`: token de `useAuth().session`; `page`, `search`, `brandId`, `categoryId`, `isActive` lidos de `useSearchParams`; `setFilter` com `router.replace` removendo `page` e parâmetros vazios; busca com atraso de ~300 ms; carregamento de marcas e categorias para os filtros; `deleteProduct` com toast, refetch e ida para a página anterior quando a página esvaziar; `listQuery` com a query string atual. Verificar no navegador (tarefa 12.5) que alterar a marca atualiza a URL e a tabela.
- [x] 9.5 Criar `use-product-form.hook.ts`: carrega produto por id (mensagem de não encontrado em 404), opções de marcas e categorias (rótulo `path`), converte centavos ↔ reais, gera `images[].order` pelo índice, submete criação/alteração, mapeia `ApiError` por código (`PRODUCT_SLUG_ALREADY_EXISTS` → `slug`, `PRODUCT_SKU_ALREADY_EXISTS` → `sku`, `BRAND_NOT_FOUND` → `brandId`, `CATEGORY_NOT_FOUND` → `categoryId`, demais → erro geral) e, em sucesso, exibe toast e volta para a lista com a query recebida. Verificar no navegador (tarefa 12.5) o erro de sku duplicado no campo.

## 10. Frontend: listagem

- [x] 10.1 Criar `components/product-list.component.tsx` (`TableCard` + `Table`; miniatura ou placeholder em `bg-surface`; nome; sku; marca; categoria (`path`); `formatPrice(priceCents)` e `formatPrice(listPriceCents)` riscado; badge "Ativo"/"Inativo"; ações editar (link com a query da lista) e excluir (`DeleteConfirmationDialog`); `EmptyListState` quando vazia), recebendo itens e callbacks por props. Verificar visualmente conforme `DESIGN.md` na tarefa 12.5.
- [x] 10.2 Criar `pages/products.page.tsx`: `PageSectionHeader` com "Novo produto" (link com a query atual), barra de filtros (busca, `Combobox` de marca, `Combobox` de categoria por `path`, seletor de status), `ProductList` e `PaginationControls` (`page`, `totalPages`, `totalItems`, `totalLabel: "produtos"`). Verificar no navegador que a primeira página mostra 20 produtos e 54 páginas com o seed.

## 11. Frontend: formulário

- [x] 11.1 Criar `components/product-form.component.tsx` com `FormSectionLayout` (sem modal), `react-hook-form` + `v.resolver(productSchema)` e as seções Identificação (`name`, `slug` automático via `Alias.format` até edição manual, `sku`), Classificação (`Combobox` de marca com "Sem marca", `Combobox` de categoria por `path`), Descrição (`Textarea`), Preço (`price`, `listPrice`, `unit` com padrão "unidade"), Imagens (`OrderableObjectList` de `{ thumbUrl, largeUrl }` com pré-visualização, mover/remover, "Adicionar imagem" desabilitado com 10 itens e marca "Principal" no primeiro) e Publicação (`isActive`, apenas na edição); botões salvar e cancelar. Verificar no navegador que digitar o nome preenche o slug e que editar o slug interrompe o preenchimento.
- [x] 11.2 Criar `pages/product-form.page.tsx` (recebe `id?` e `returnQuery`, usa `useProductForm`, `FormSkeleton` durante o carregamento). Verificar no navegador que abrir `/admin/catalog/products/<id>` carrega o preço em reais.

## 12. Frontend: rotas, menu, marcas/categorias e validação final

- [x] 12.1 Criar dentro do grupo `(shell)`: `app/admin/(shell)/catalog/products/page.tsx` (envolve `ProductsPage` em `<Suspense>`), `products/new/page.tsx` e `products/[id]/page.tsx` (aguardam `params`/`searchParams` e repassam `id` e a query de retorno); adicionar `CATALOG_PRODUCTS_ROUTE` e `catalogProductRoute(id)` em `src/shared/navigation/catalog-routes.ts` e exportar páginas em `modules/catalog/index.ts`. Verificar que as três rotas renderizam logado como administrador.
- [x] 12.2 Adicionar em `src/shared/navigation/app-modules.ts` o item `{ id: 'catalog-products', label: 'Produtos', icon: Package, href: CATALOG_PRODUCTS_ROUTE, match: 'prefix' }` na seção `catalog-registrations`, após "Categorias". Verificar no navegador que "Produtos" fica ativo em `/admin/catalog/products/new`.
- [x] 12.3 Nas telas de marcas e categorias, tratar o `ApiError` com status `409` da exclusão exibindo `toast.error(error.message)`, sem remover o item da lista. Verificar no navegador que excluir uma marca do seed com produtos mostra "Marca possui produtos cadastrados" e que excluir uma categoria folha com produtos mostra "Categoria possui produtos cadastrados".
- [x] 12.4 Rodar `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend`. Verificar que ambos terminam sem erros (incluindo o aviso de `useSearchParams` sem `Suspense`).
- [x] 12.5 Conferir no navegador, logado como `usuario@formacao.dev`: listagem paginada com os produtos do seed; filtros de busca, marca, categoria (com subcategorias) e status refletidos na URL e mantidos após recarregar; criação com duas imagens e preço "De:"; validação no cliente de preço "De:" menor; sku duplicado exibido no campo; edição reordenando imagens e voltando para a mesma página filtrada; exclusão com confirmação e toast. Registrar capturas de tela da lista filtrada e do formulário.

## 13. Ajustes no menu do Catálogo (pedido após a entrega)

- [x] 13.1 Renomear o módulo para "Catálogo de Produtos" em `src/shared/navigation/app-modules.ts` (e o título da visão geral em `catalog-dashboard.component.tsx`) e remover o rótulo "Cadastros" da seção `catalog-registrations`, mantendo "Visão geral", "Marcas", "Categorias" e "Produtos" na mesma ordem. Verificar no DOM da sidebar que nenhum rótulo "Cadastros" é renderizado.
- [x] 13.2 Em `src/shared/components/ui/sidebar-menu.component.tsx`, destacar um único item por vez: com sub-item ativo, o item principal fica só expandido (texto claro, sem fundo laranja e sem `aria-current`). Verificar no navegador que em `/admin/catalog/products/new` só "Produtos" tem `aria-current="page"`, em `/admin/catalog` só "Visão geral", e em `/admin/orders` o item "Pedidos" continua destacado.
- [x] 13.3 Atualizar as specs (deltas `admin/admin-area`, `catalog/brand-management` e `catalog/product-admin` deste change e o delta `admin/catalog-categories` do change `cadastro-categoria`). Verificar com `openspec validate --strict` dos dois changes, `npm run lint --workspace=@jaja/frontend` e `npx tsc --noEmit` no frontend sem erros.
