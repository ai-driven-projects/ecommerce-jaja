> Execução: grupos 1 (Negócio), 2–3 (Backend) e 4–6 (Frontend) em subagentes separados, com contexto limpo, **nessa ordem**. Cada subagente lê `.claude/skills/skills-standards.md`; o de frontend lê também `apps/frontend/DESIGN.md`.

## 0. Pré-requisito

- [x] 0.1 Confirmar que o prompt 06 está implementado: existem `apps/backend/src/shared/decorators/admin-only.decorator.ts` (exportado em `shared/decorators/index.ts`) e `apps/backend/src/shared/auth/admin.guard.ts`, e `apps/frontend/src/shared/navigation/app-modules.ts` tem em `sectionsByModuleId.catalog` as seções `catalog-overview` ("Visão geral") e `catalog-registrations` ("Cadastros"). Se algo faltar, **parar** e aplicar o prompt 06 antes. Verificar com `grep -rn "AdminOnly\|catalog-registrations" apps/backend/src apps/frontend/src`.

## 1. Domínio `@jaja/catalog` (subagente Negócio)

- [x] 1.1 Remover `modules/catalog/src/catalog`, `modules/catalog/test/catalog` e `modules/catalog/test/mock/in-memory-catalog.repository.ts`, deixando `modules/catalog/src/index.ts` só com `getModuleName()`. Verificar que `test/index.test.ts` continua passando com `npm test --workspace=@jaja/catalog`.
- [x] 1.2 Gerar o agregado com a skill `module-aggregate` (`brand`, `--mode example`) e apagar o use case e o teste de exemplo gerados, mantendo `src/brand/{model,provider,dto}` e `test/mock/in-memory-brand.repository.ts`. Verificar com `find modules/catalog/src/brand modules/catalog/test -type f` que não resta arquivo de use case/teste de exemplo.
- [x] 1.3 Implementar `src/brand/model/brand.entity.ts` (skill `module-entity`): props `id`, `name` (`Name`), `slug` (`Alias`, derivado com `Alias.format(name)` quando vazio), `description` (`Text` `{ optional: true, maxLength: 500 }`), `logoUrl` (`Url` `{ optional: true }`), `isActive` (`Flag`, padrão `true`); `Result.combine`, `create`/`tryCreate` e getters. Verificar em `test/brand/brand.entity.test.ts`: atributos válidos, `isActive` padrão `true`, nome vazio falha, slug `"Acme Marca"` falha, `{ name: "Café & Cia" }` gera `cafe-cia`, `logoUrl: "ftp://x"` falha, descrição de 501 caracteres falha.
- [x] 1.4 Implementar `src/brand/provider/brand.repository.ts` (skill `module-repository`): `BrandRepository extends CrudRepository<Brand>` com `findBySlug(slug): Promise<Result<Brand | null>>` e `findByName(name): Promise<Result<Brand | null>>`; exportar a constante de erro `BRAND_NOT_FOUND`. Ajustar o mock in-memory ao contrato: `findById` falha com `BRAND_NOT_FOUND`, `findByName` ignora caixa, `delete` preenche `deletedAt` e nenhuma busca retorna excluídos. Verificar com `npx tsc --noEmit -p modules/catalog`.
- [x] 1.5 Implementar `src/brand/dto/brand.dto.ts` (skill `module-dto`) com `id`, `name`, `slug`, `description`, `logoUrl`, `isActive`, `createdAt`, `updatedAt`, e o mapeamento entidade → DTO usado pelo use case. Verificar com `npx tsc --noEmit -p modules/catalog`.
- [x] 1.6 Implementar `src/brand/provider/find-brands.query.ts` (`FindBrandsQuery.execute({ search?, isActive? }): Promise<Result<BrandDTO[]>>`) e `find-brand-by-id.query.ts` (`FindBrandByIdQuery.execute(id): Promise<Result<BrandDTO | null>>`) (skill `module-query-cqrs`), exportadas no barrel de `provider`. Verificar com `npx tsc --noEmit -p modules/catalog`.
- [x] 1.7 Implementar `src/brand/use-case/save-brand.use-case.ts` (skill `module-use-case`) conforme a Decisão 4 do `design.md`: `BRAND_NOT_FOUND` → criação (uuid v4 quando sem `id`, `BRAND_NAME_ALREADY_EXISTS`, slug derivado, `BRAND_SLUG_ALREADY_EXISTS`, `isActive` padrão `true`); marca existente → alteração com as mesmas checagens ignorando a própria marca, `cloneWith`, `undefined` mantém o valor atual e `null`/`''` limpa `description`/`logoUrl`; outras falhas de `findById` são propagadas; retorna `BrandDTO`. Verificar em `test/brand/save-brand.use-case.test.ts`: criação sem `id` gera uuid e slug; criação com `id` informado usa esse id; nome duplicado em outra caixa; slug duplicado; alteração mantendo o próprio nome/slug sem conflito; alteração sem `slug` mantém o slug atual; alteração para o nome de outra marca falha.
- [x] 1.8 Implementar `src/brand/use-case/delete-brand.use-case.ts`: `findById` (propaga `BRAND_NOT_FOUND`) e `delete`. Verificar em `test/brand/delete-brand.use-case.test.ts`: exclusão faz `findById` passar a falhar com `BRAND_NOT_FOUND` e `findBySlug` retornar `null`; excluir id inexistente falha com `BRAND_NOT_FOUND`.
- [x] 1.9 Exportar `brand` em `src/brand/index.ts` e `src/index.ts`. Verificar que `npm test --workspace=@jaja/catalog` passa e que `npm run build --workspace=@jaja/catalog` gera `dist/index.d.ts` contendo `Brand`, `BrandRepository`, `BrandDTO`, `FindBrandsQuery`, `FindBrandByIdQuery`, `SaveBrand` e `DeleteBrand`, sem `Catalog`.

## 2. Backend: banco e adapter (subagente Backend)

- [x] 2.1 Mapear em `apps/backend/prisma/models/catalog.model.prisma` o model `Brand` (skill `backend-prisma-data`): `id String @id @db.Uuid`, `name String @unique`, `slug String @unique`, `description String?`, `logoUrl String? @map("logo_url")`, `isActive Boolean @default(true) @map("is_active")`, `createdAt`/`updatedAt`/`deletedAt DateTime?` com `@map` snake_case e `@@map("brands")`. Rodar `npm run prisma:migrate:dev --workspace=@jaja/backend -- --name catalog_brand` e `npm run prisma:generate --workspace=@jaja/backend`. Verificar que a migration criada só contém a tabela `brands` e os índices únicos `brands_name_key` e `brands_slug_key`.
- [x] 2.2 Implementar `apps/backend/src/modules/catalog/brand.prisma.ts` (`BrandPrisma implements BrandRepository`) no padrão de `user.prisma.ts`: `toDomain`/`fromDomain`, `Result.tryAsync`, `clientFor(tx)`, `delete` como soft delete, todas as leituras com `deletedAt: null`, `findById` falhando com `BRAND_NOT_FOUND`, `findByName` com `mode: 'insensitive'`, e tradução de `P2002` em `create`/`update` (`name` → `BRAND_NAME_ALREADY_EXISTS`, `slug` → `BRAND_SLUG_ALREADY_EXISTS`, `id` → `BRAND_NOT_FOUND`, checando `meta.target` e o nome da constraint). Verificar que `npm run build --workspace=@jaja/backend` compila (comportamento validado na tarefa 3.4).
- [x] 2.3 Adicionar em `BrandPrisma` os atributos públicos `findBrands: FindBrandsQuery` (`search` com `OR` de `contains`/`insensitive` em `name` e `slug`, `isActive` opcional, ordenação por nome sem distinção de maiúsculas feita no adapter com collator `pt-BR`) e `findBrandById: FindBrandByIdQuery` (`null` quando não existir ou estiver excluída), mapeando linhas direto para `BrandDTO`. Verificar que o backend compila.
- [x] 2.4 Remover `catalog.prisma.ts` e `catalog.controller.ts`; em `catalog.module.ts` trocar `CatalogPrisma` por `BrandPrisma` em `providers`/`exports` e registrar `BrandController` (criado em 3.1). Verificar com `grep -rn "CatalogPrisma\|CatalogController" apps/backend/src` sem resultados e backend compilando após 3.1.

## 3. Backend: API, seed e integração (subagente Backend)

- [x] 3.1 Criar `apps/backend/src/modules/catalog/brand.controller.ts` (skill `backend-controller`) com `@Controller('brands')` e `@AdminOnly()` na classe: `POST /brands` (201, `SaveBrand` sem `id`), `GET /brands` (`findBrands`, `isActive` só quando `"true"`/`"false"`), `GET /brands/:id` (`findBrandById`, `404` em `null`), `PUT /brands/:id` (`SaveBrand` com o `id` da rota), `DELETE /brands/:id` (204, `DeleteBrand`); mapear `BRAND_NOT_FOUND` → `NotFoundException`, conflitos → `ConflictException`, demais → `BadRequestException` com códigos deduplicados. Verificar que `npm run build --workspace=@jaja/backend` compila.
- [x] 3.2 Criar `apps/backend/prisma/seed/tasks/catalog-brands.seed.ts` exportando `seedCatalogBrands` (Decisão 8: caminho resolvido a partir de `import.meta.url`, erro claro com o caminho quando o arquivo não existe, `Alias.format`, consolidação por slug mantendo o primeiro nome, `upsert` por `slug` com `update: {}`) e registrá-lo em `seedTasks` de `prisma/seed/main.ts` após `seedAuth`. Verificar que `apps/cli/data/kalunga/brands.json` não aparece em `git status` e que nada foi criado em `prisma/seed/data`.
- [x] 3.3 Rodar `npm run prisma:seed --workspace=@jaja/backend` duas vezes. Verificar que a tabela `brands` tem 210 registros após cada execução, com uma linha para `spiral`, `hp` e `trident` e a marca "Make+" com slug `make` (consulta SQL no container ou `prisma studio`).
- [x] 3.4 Criar `apps/backend/src/modules/catalog/test/brand.integration.http` no estilo de `auth/test/auth.integration.http` (tokens via `POST /auth/login` de `usuario@formacao.dev` e de um usuário do seed com `admin: false`, senha `#Senha123`; nomes com sufixo `{{$timestamp}}`), cobrindo: 401 sem token, 403 não administrador, criação 201, nome duplicado 409, slug duplicado 409, logo inválida 400, listagem com `search` 200, busca por id 200 e 404, alteração 200, exclusão 204 seguida de 404, criação com o nome da marca excluída 409 e `PUT` no id excluído 404. Subir o backend (`npm run dev --workspace=@jaja/backend`, porta 4000) e verificar que todas as chamadas retornam os status esperados.
- [x] 3.5 Verificar que `npm run test --workspace=@jaja/backend` e `npm run build --workspace=@jaja/backend` passam sem erros e que `POST /auth/login` continua respondendo 200.

## 4. Frontend: base compartilhada (subagente Frontend)

- [x] 4.1 Criar `apps/frontend/src/shared/util/api-client.util.ts` movendo literalmente `describeApiError`, `ApiErrorPayload`, `toStringList`, `extractDetails`, o dicionário de códigos e as mensagens genérica/de rede de `src/modules/auth/data/auth.api.ts`; adicionar `ApiError extends Error` (`status`, `codes`) e `apiRequest<T>(path, { method?, token?, body? })` (`NEXT_PUBLIC_API_URL`, `Content-Type` só com corpo, `Authorization: Bearer` só com token, `undefined` em 204, `ApiError` em resposta com erro, `Error` legível em falha de rede). `auth.api.ts` passa a importar de lá, sem outra mudança. Verificar que `npm run lint --workspace=@jaja/frontend` passa e que `grep -n "function describeApiError" -r apps/frontend/src` só encontra o novo arquivo.
- [x] 4.2 Adicionar `BRAND_NOT_FOUND`, `BRAND_NAME_ALREADY_EXISTS` e `BRAND_SLUG_ALREADY_EXISTS` em `src/shared/i18n/messages.pt.ts` e `messages.en.ts`. Verificar que os dois arquivos têm as três chaves e o frontend compila.
- [x] 4.3 Adicionar `CATALOG_BRANDS_ROUTE` e `catalogBrandRoute(id)` em `src/shared/navigation/catalog-routes.ts` e o item `{ id: 'catalog-brands', label: 'Marcas', href: CATALOG_BRANDS_ROUTE, icon: Tag, match: 'prefix' }` na seção `catalog-registrations` de `app-modules.ts`. Verificar no lint/build e, na tarefa 6.5, que o menu exibe "Visão geral", "Cadastros" e "Marcas".

## 5. Frontend: dados do catálogo (subagente Frontend)

- [x] 5.1 Criar `src/modules/catalog/data/brand.api.ts` com `listBrands(token, filter)`, `getBrand(token, id)`, `createBrand(token, data)`, `updateBrand(token, id, data)` e `deleteBrand(token, id)` sobre `apiRequest`, e o tipo da marca retornada pela API. Verificar que o frontend compila.
- [x] 5.2 Criar `src/modules/catalog/data/brand.schema.ts` (skill `frontend-form-schema`) com `v.defineObject` usando `Name`, `Alias`, descrição opcional até 500 caracteres, `Url` opcional e `isActive`, e `BrandFormData = v.infer<typeof brandSchema>`. Verificar que o frontend compila e que o schema rejeita slug `"Acme Marca"` e `logoUrl: "logo"`.
- [x] 5.3 Criar `src/modules/catalog/data/use-brands.hook.ts` (token de `useAuth().session`, lista, `search` com debounce, `isActive`, `loading`, `remove(id)` com toaster de sucesso e recarga) e `use-brand-form.hook.ts` (carrega por id na edição, submit de criação/alteração, `ApiError` 409 → `setError` em `name`/`slug` pelo código, demais erros em toaster, sucesso → toaster + `router.push(CATALOG_BRANDS_ROUTE)`); exportar tudo em `data/index.ts`. Verificar que `npm run lint --workspace=@jaja/frontend` passa.

## 6. Frontend: telas e rotas (subagente Frontend)

- [x] 6.1 Criar `src/modules/catalog/components/brand-list.component.tsx` que recebe a lista por prop e renderiza `TableCard` + `Table` (miniatura do logo ou placeholder `bg-surface`, nome, slug, badge "Ativa"/"Inativa", ações editar/excluir) e `EmptyListState` quando vazia; a exclusão abre `DeleteConfirmationDialog`. Verificar que o frontend compila.
- [x] 6.2 Criar `src/modules/catalog/pages/brands.page.tsx` com `PageSectionHeader`, campo de busca, botão "Nova marca" (link para `/admin/catalog/brands/new`) e `BrandList` alimentado por `useBrands`. Verificar que o frontend compila.
- [x] 6.3 Criar `src/modules/catalog/components/brand-form.component.tsx` em página (sem modal) com `FormSectionLayout`, `react-hook-form` + `v.resolver(brandSchema)`, campos nome, slug (automático via `Alias.format` enquanto não editado; já considerado editado no modo edição), descrição (`Textarea`), URL do logo com pré-visualização quando `Url.isValid`, e `isActive` (`Checkbox`) só na edição; e `src/modules/catalog/pages/brand-form.page.tsx` recebendo `id?` e usando `useBrandForm`. Verificar que o frontend compila.
- [x] 6.4 Criar as rotas em `src/app/admin/(shell)/catalog/brands/page.tsx`, `brands/new/page.tsx` e `brands/[id]/page.tsx` (esta aguarda `params` e repassa `id` para `BrandFormPage`) e exportar componentes e páginas novos em `src/modules/catalog/index.ts`. Verificar que `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend` passam sem erros e que o build lista as três rotas.
- [x] 6.5 Com banco semeado, backend e frontend no ar, entrar como `usuario@formacao.dev` e verificar no navegador: menu do Catálogo com "Visão geral", "Cadastros" e "Marcas" (ativo nas rotas de marcas); lista com 210 marcas e busca; criação com slug automático e toaster; conflito de nome exibido no campo; edição desmarcando "ativa" com volta à lista mostrando "Inativa"; exclusão com confirmação e toaster; e que `/entrar` e `/admin/login` continuam exibindo "Email ou senha inválidos" e "Este email já está cadastrado".
