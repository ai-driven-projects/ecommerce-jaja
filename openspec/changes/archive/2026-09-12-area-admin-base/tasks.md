## 1. Backend: `AdminGuard` e `@AdminOnly()` (subagente com contexto limpo; ler `.claude/skills/skills-standards.md`)

- [x] 1.1 Criar `apps/backend/src/shared/auth/admin.guard.ts` (`@Injectable()`, `implements CanActivate`): lê `context.switchToHttp().getRequest<AuthenticatedRequest>().user`; sem `user` lança `UnauthorizedException`; com `user.admin !== true` lança `ForbiddenException('ADMIN_REQUIRED')`; senão retorna `true`. Exportar em `shared/auth/index.ts` e adicionar `AdminGuard` a `providers` e `exports` de `shared/shared.module.ts`, sem registrar nenhum `APP_GUARD`. Verificar que `grep -rn APP_GUARD apps/backend/src` não retorna nada e que `npm run build --workspace=@jaja/backend` passa.
- [x] 1.2 Criar `apps/backend/src/shared/auth/admin.guard.spec.ts` (Vitest, `ExecutionContext` falso no estilo do `createHost` de `shared/errors/api-exception.filter.spec.ts`) com três casos: `admin: true` → `true`; `admin: false` → `ForbiddenException`; sem `request.user` → `UnauthorizedException`. Verificar que `npm run test --workspace=@jaja/backend` passa com os três casos.
- [x] 1.3 Criar `apps/backend/src/shared/decorators/admin-only.decorator.ts` exportando `AdminOnly = () => applyDecorators(UseGuards(JwtGuard, AdminGuard))`, nessa ordem, importando `../auth/jwt.guard.js` e `../auth/admin.guard.js` (não os `index`). O JSDoc diz que o decorator vale para classe e método, que responde `401` sem token válido e `403` (`ADMIN_REQUIRED`) para não administrador e que não deve ser combinado com `@Public()`. Exportar em `shared/decorators/index.ts`. Verificar com `grep -n "from '../auth" apps/backend/src/shared/decorators/admin-only.decorator.ts` que só aparecem imports por arquivo, e que o build passa.
- [x] 1.4 Criar `apps/backend/src/shared/decorators/admin-only.decorator.spec.ts`: aplicar `@AdminOnly()` a uma classe de teste e esperar `Reflect.getMetadata(GUARDS_METADATA, TestController)` (`GUARDS_METADATA` de `@nestjs/common/constants`) igual a `[JwtGuard, AdminGuard]`. Verificar que `npm run test --workspace=@jaja/backend` passa.
- [x] 1.5 Validar o backend sem regressão: `npm run test` e `npm run build --workspace=@jaja/backend` sem erros; com banco (`npm run db:start --workspace=@jaja/backend`) e backend no ar (`npm run dev --workspace=@jaja/backend`, porta 4000), conferir que `POST /auth/login` responde `200` para `admin@jaja.dev` (`admin: true`) e `ana.pereira.carvalho@jaja.dev` (`admin: false`), senha `#Senha123`; que `GET /auth/me` responde `200` com o token de cada um e `401` sem token; e que `GET /` sem token responde como antes. Verificar com `git diff --stat apps/backend` que só mudaram `shared.module.ts`, `shared/auth/index.ts` e `shared/decorators/index.ts`, além dos quatro arquivos novos.

## 2. Frontend: seções na sidebar (subagente com contexto limpo; ler `.claude/skills/skills-standards.md` e `apps/frontend/DESIGN.md`)

- [x] 2.1 Ajustar `apps/frontend/src/shared/components/ui/sidebar-menu.component.tsx`: calcular as seções com itens (`items.length > 0`) e exibir os sub-itens só quando a soma delas for maior que 1. Sob o item principal ativo, renderizar cada seção em ordem como `div` com `role="group"` (chave `section.id`). Quando houver `label`, exibir um cabeçalho de texto não interativo, ligado por `aria-labelledby`, com `pb-1 pl-11 pr-[13px] pt-2 text-xs font-extrabold uppercase tracking-[0.04em] text-dark-muted` e sem ícone. Os itens usam `SidebarItemLink nested`. Não alterar `SidebarItemLink`, `isSidebarItemActive`, os tipos exportados nem a renderização dos itens principais e do `badge`. Atualizar o comentário da prop `sections`. Verificar que `npm run lint --workspace=@jaja/frontend` passa.
- [x] 2.2 Em `apps/frontend/src/shared/navigation/app-modules.ts`, definir `sectionsByModuleId.catalog` com `{ id: 'catalog-overview', items: [{ id: 'catalog-overview', label: 'Visão geral', href: CATALOG_ROUTE, match: 'exact' }] }` e `{ id: 'catalog-registrations', label: 'Cadastros', items: [] }`, este com um comentário dizendo que os prompts 07, 08 e 09 adicionam "Marcas", "Categorias" e "Produtos", nessa ordem, com `match: 'prefix'`. Os demais módulos continuam `[]`. Verificar que `npm run lint` e `npm run build --workspace=@jaja/frontend` passam sem erros.
- [x] 2.3 Validar no navegador (`npm run dev --workspace=@jaja/frontend`, backend no ar), logado como `usuario@formacao.dev` / `#Senha123`: em `/admin`, `/admin/orders`, `/admin/catalog`, `/admin/couriers`, `/admin/customers` e `/admin/stores`, a sidebar mostra só os itens principais, com o módulo correto ativo, igual ao antes. Adicionar temporariamente em "Cadastros" um item fictício (`{ id: 'catalog-dummy', label: 'Item fictício', href: `${CATALOG_ROUTE}/dummy`, match: 'prefix' }`) e conferir em `/admin/catalog` que "Visão geral" (ativo), o rótulo "Cadastros" em caixa alta discreta alinhado aos sub-itens e "Item fictício" aparecem sob "Produtos & estoque". **Remover o item fictício** e verificar com `git diff apps/frontend/src/shared/navigation/app-modules.ts` que "Cadastros" termina com `items: []`, e que o lint e o build passam de novo.

## 3. Fechamento

- [x] 3.1 Rodar `openspec validate area-admin-base --strict` sem erros e verificar com `git diff --stat` que não mudaram `apps/backend/src/modules/auth/auth.controller.ts`, `apps/backend/src/shared/auth/jwt.guard.ts`, `apps/frontend/src/modules/auth/components/admin-guard.component.tsx`, `apps/frontend/src/app/admin/login`, `apps/frontend/src/app/admin/(shell)/layout.tsx` nem `apps/frontend/src/shared/components/store`.
