## 1. Base visual: documento, fontes e tokens

- [x] 1.1 Copiar `openspec/extras/design/docs/design.md` para `apps/frontend/DESIGN.md` e adicionar em `apps/frontend/AGENTS.md` (fora do bloco gerado pelo Next) a instrução "toda tela e componente compartilhado segue `DESIGN.md`". Verificar que os dois arquivos existem e que `AGENTS.md` referencia `DESIGN.md`.
- [x] 1.2 Em `apps/frontend/src/app/layout.tsx`, substituir Geist por `Archivo` (400/600/800), `Bricolage_Grotesque` (eixo opsz, 600–800) e `IBM_Plex_Mono` (400/600) de `next/font/google` com `variable` `--font-ui`, `--font-display` e `--font-mono`; atualizar `metadata` para título `já já.` e descrição do e-commerce hiperlocal; manter `lang="pt-BR"` e não montar `<Toaster />`. Verificar com `npm run dev --workspace=@jaja/frontend` que o `<html>` recebe as três classes de variável e a aba mostra "já já.".
- [x] 1.3 Reescrever os tokens de `apps/frontend/src/app/globals.css`: declarar `--bg`, `--surface`, `--ink`, `--accent`, `--muted-ink`, `--rule-soft`, `--placeholder`, `--accent-text`; remapear `--background`, `--foreground`, `--card`, `--popover`, `--primary`, `--primary-foreground`, `--secondary`, `--muted`, `--accent`(shadcn→`--surface`), `--muted-foreground`, `--border`, `--input`, `--ring`, `--destructive`, `--chart-*` e `--radius: 0`; remover o bloco `prefers-color-scheme: dark` e o `@import 'tw-animate-css'`; no `@theme` adicionar `--color-brand`, `--color-paper`, `--color-ink`, `--color-rule-soft`, `--color-placeholder`, `--font-sans: var(--font-ui)`, `--font-mono`, `--font-display`. Verificar que `grep -n "dark\|tw-animate" globals.css` retorna vazio e a página abre em fundo `#f3f2f2`.
- [x] 1.4 No `@layer base` de `globals.css`: `body` em papel/tinta/`--font-ui`, `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px }`, `::selection` em `rgba(236,48,19,0.2)`, `a:hover { color: var(--accent) }`, `button { text-align: left }`. Verificar no navegador que Tab em um link mostra contorno vermelho e que hover em link fica vermelho.
- [x] 1.5 Remover `tw-animate-css` das `devDependencies` de `apps/frontend/package.json` (`npm uninstall tw-animate-css -w @jaja/frontend`) e apagar `file.svg`, `globe.svg`, `next.svg`, `vercel.svg` e `window.svg` de `apps/frontend/public`. Verificar que `npm run build --workspace=@jaja/frontend` conclui sem erro.

## 2. Componentes compartilhados de UI (manter props e comportamento)

- [x] 2.1 Restilizar `button.tsx`: base sem `rounded-*`, `justify-start`, `transition-colors duration-100`; `default` = `bg-ink text-paper hover:bg-black`; `outline` = `border-2 border-ink bg-transparent hover:bg-surface`; `secondary` = `bg-surface text-ink`; `ghost` = `hover:text-brand`; tamanhos sem radius. Verificar renderizando os quatro variants em `/principal` (temporariamente) ou via grep de que não resta `rounded-`.
- [x] 2.2 Restilizar `input.tsx` e `textarea.tsx` (`bg-surface border-0 border-b-2 border-ink rounded-none`, sem `ring`, foco pelo outline global) e `label.tsx` (`text-[11px] uppercase tracking-[0.08em] text-muted-foreground`). Verificar visualmente um campo com label em `/join` ou em um dashboard privado (fundo `#eae9e9`, borda inferior 2px).
- [x] 2.3 Restilizar `card.tsx` (`border-2 border-ink rounded-none`, sem sombra), `badge.tsx` (`border-2 rounded-none`, nova variante `brand` com `border-brand text-[#b3260f]`), `separator.tsx` (`bg-ink`, horizontal `h-0.5`, vertical `w-0.5`) e `tabs.tsx` (lista sem fundo/radius; trigger com `underline underline-offset-4 decoration-1`, ativo `decoration-2 decoration-brand font-semibold`). Verificar que nenhum dos quatro arquivos contém `rounded-md|shadow|bg-muted p-1`.
- [x] 2.4 Restilizar `table.tsx`: régua `border-t-2 border-b-2 border-ink` na tabela, linhas com `border-b border-rule-soft`, cabeçalho em caixa alta 11px, células numéricas com `font-mono tabular-nums` quando `align="right"`. Verificar com uma tabela de exemplo (ex.: `table-card.tsx` em um dashboard) mostrando réguas fortes acima/abaixo e finas entre linhas.
- [x] 2.5 Restilizar `dialog.tsx`, `sheet.tsx`, `popover.tsx`, `dropdown-menu.tsx` e `tooltip.tsx`: overlay `bg-[rgba(32,30,29,0.35)]`, conteúdo `bg-paper border-2 border-ink rounded-none shadow-none`, remover todas as classes `animate-*`, `fade-*`, `zoom-*`, `slide-*`, `duration-*` de entrada e `transition ease-in-out`. Verificar que `grep -rn "animate-\|shadow-\|rounded-\|blur" apps/frontend/src/shared/components/ui/{dialog,sheet,popover,dropdown-menu,tooltip}.tsx` retorna vazio e que abrir o sheet mobile em `/principal` não anima.
- [x] 2.6 Restilizar `form-error-message.tsx` (`text-[#b3260f]`, sem `dark:`), `form-skeleton.tsx` (blocos `bg-placeholder` estáticos, `rounded-none`, sem `animate-pulse`), `toaster.tsx` (Sonner com `toastOptions.classNames` para fundo papel, borda 2px, sem radius/sombra, `richColors` desligado), `empty-dashboard-state.tsx`, `empty-list-state.tsx`, `page-section-header.tsx` e `section-header.tsx` (divisores `border-b-2 border-ink`). Verificar com grep que não resta `animate-pulse|dark:|rounded-` nesses arquivos.
- [x] 2.7 Revisar os demais arquivos de `shared/components/ui` (`calendar`, `checkbox`, `color-input`, `combobox`, `icon-combobox`, `date-picker-input`, `delete-confirmation-dialog`, `standard-dialog-content`, `metric-card`, `mini-form-card`, `navigation-link-card`, `orderable-object-list`, `pagination-controls`, `radio-group`, `readonly-text-field`, `table-card`, `dashboard-*-card`, `lucide-icon-by-key`) trocando `rounded-*`, `shadow-*`, gradientes e `animate-*` pelo padrão flat, e nos gráficos (`composed-bar-line-chart`, `pie-breakdown-chart`) usar apenas `--ink`, `--accent`, `--muted-ink`, `--rule-soft`. Verificar que `grep -rn "animate-\|shadow-\|rounded-\|gradient\|blur" apps/frontend/src/shared/components/ui` retorna vazio (exceto `rounded-none`).
- [x] 2.8 Reescrever `branding/app-logo.component.tsx`: `AppWordmark` renderiza `já já` + `<span className="text-brand">.</span>` em `font-extrabold lowercase tracking-[-0.5px]`; `AppLogoMark` renderiza a forma reduzida `jj.` na mesma tipografia sem `next/image`; `AppLogo` mantém a assinatura (`size`, `showMark`, `showText`, `withText`, `priority`, classes). Verificar que o arquivo não importa `next/image` e que a sidebar em `/principal` mostra "já já." e, recolhida, "jj.".

## 3. Shell administrativo flat

- [x] 3.1 Restilizar `template/admin-shell.component.tsx`: container `bg-paper text-ink`, aside `border-r-2 border-ink` sem gradientes/`shadow-[inset...]`, remover as `div` de gradiente decorativo, header `border-b-2 border-ink bg-paper` sem `backdrop-blur`, avatar `border-2 border-ink` sem `rounded-full`, item de logout em `text-[#b3260f]`. Manter `useShell`, `Sheet` mobile, `DropdownMenu` e todas as props. Verificar em `/principal` que sidebar e header aparecem em papel separados por linhas de 2px e que recolher/expandir a sidebar continua funcionando.
- [x] 3.2 Restilizar `components/ui/sidebar-menu.component.tsx`: constantes `ITEM_BASE_CLASS`, `ACTIVE_CLASS`, `MODULE_RAIL_ITEM_CLASS` e classes inline sem `rounded-*`, `bg-white/*`, `text-zinc-*`, gradientes ou sombras; item ativo com `border-l-2 border-brand font-semibold`; divisores `border-b-2 border-ink`/`bg-ink h-0.5`; popover do módulo recolhido em papel com borda 2px. Manter a lógica de `isItemActive`, seções, rail e flyout. Verificar que a rota ativa mostra borda esquerda vermelha e os demais itens não, no modo expandido e recolhido.

## 4. Componentes reutilizáveis da loja

- [x] 4.1 Criar `shared/util/price.util.ts` com `formatPrice(cents: number): string` (`R$ 12,90`, `R$ 0,50`) e `shared/components/store/price.component.tsx` (`font-mono tabular-nums`). Verificar com um teste rápido em node (`node -e`) ou log que 1290 → `R$ 12,90`, 50 → `R$ 0,50` e 100000 → `R$ 1000,00`.
- [x] 4.2 Criar `shared/components/store/store.types.ts` (`StoreProduct { slug, name, category, priceCents, unit }`, `BagItem { productId, name, priceCents, quantity }`, `Zone { neighborhood, hub }`) e `category-image.component.tsx` portando os cinco desenhos de `ImagemCategoria.tsx` (traço `stroke="var(--ink)"` 3px, `role="img"`, `aria-label` = categoria, fallback para tecnologia básica). Verificar renderizando as cinco categorias e uma desconhecida.
- [x] 4.3 Criar `storefront-header.component.tsx` com props `neighborhood`, `neighborhoods`, `etaMinutes: number | null`, `bagCount`, `onNeighborhoodChange`, `onOpenBag` e `bagButtonRef` opcional: header `flex flex-wrap items-center gap-x-7 gap-y-3 px-6 py-3.5 border-b-2 border-ink`, `AppLogo` como link para `/`, `<select>` com `border-0 border-b-2 border-ink bg-transparent font-semibold`, ETA `ml-auto font-mono tabular-nums text-lg` com `<b className="text-brand">X min</b>` omitido quando `etaMinutes` é `null`, botão com `ShoppingBag` (lucide, `strokeWidth={2}`) e contador `font-mono border-2 border-ink px-[7px] leading-5`. Verificar visualmente contra o header de `Vitrine.dc.html`.
- [x] 4.4 Criar `category-filters.component.tsx` (props `categories`, `active`, `onChange`; `flex flex-wrap gap-x-[22px] gap-y-2.5 px-6 py-3.5 border-b-2 border-ink`; botão `underline underline-offset-4 decoration-1 hover:text-brand`; ativo `font-semibold decoration-2 decoration-brand`). Verificar que trocar `active` move o sublinhado vermelho.
- [x] 4.5 Criar `product-card.component.tsx` (props `product`, `href`; `block bg-paper hover:bg-surface`; imagem `bg-surface aspect-[4/3] flex items-center justify-center [&_svg]:w-[62%]` que passa a `bg-paper` no hover do card; info com categoria `text-[11px] uppercase tracking-[0.08em] text-muted-foreground`, nome `font-semibold text-[15px] leading-tight`, `Price`) e `product-grid.component.tsx` (`grid grid-cols-2 min-[900px]:grid-cols-4 gap-0.5 bg-ink border-b-2 border-ink`). Verificar com 8 produtos: 4 colunas a 1280px, 2 colunas a 375px, linhas de 2px entre células.
- [x] 4.6 Criar `bag-sheet.component.tsx` sobre o `Sheet` compartilhado: props `open`, `onClose`, `items: BagItem[]`, `etaMinutes`, `onChangeQuantity`, `onCheckout`; `SheetContent side="right"` com `w-[min(420px,100vw)] max-w-none border-l-2 border-ink p-0 flex flex-col`; cabeçalho "sacola" caixa alta + botão `×` com `aria-label="Fechar sacola"`; vazio → `Sua sacola está vazia. Já já enche.` sem rodapé; com itens → lista com controle `− n +` (botões 36px, `border-2 border-ink`, mono) e subtotal, rodapé `border-t-2` com total mono 22px, "chega em X min" e botão primário "Fechar pedido". Verificar: abre com foco no painel, `Escape` e clique no overlay fecham e o foco volta ao botão da sacola; com um item 1290×2 mostra `R$ 25,80`.
- [x] 4.7 Criar `shared/template/storefront-layout.component.tsx` (props `header?: ReactNode`, `children`; `min-h-screen bg-paper text-ink`, largura total, sem caixa/gradiente), fazer `public-boxed-layout.component.tsx` renderizar `StorefrontLayout` sem header mantendo o export `PublicBoxedLayout`, e exportar os novos componentes em `shared/template/index.ts` e `shared/index.ts` (`components/store/*`, `util/price.util`). Verificar que `PublicGroupLayout` continua tratando `/auth` sem layout e que `import { StorefrontHeader, BagSheet, ProductGrid } from '@/shared'` compila.

## 5. Vitrine (módulo `catalog` do frontend)

- [x] 5.1 Criar `modules/catalog/data/storefront.types.ts` (`Category`, `Product`, `Zone`) e `storefront.mock.ts` com `CATEGORIES` (`todas` + as 5), `ZONES` (Aldeota/Meireles/Centro → `Hub Aldeota`; Cocó/Dionísio Torres → `Hub Cocó`), `UNSERVED_NEIGHBORHOODS` (`Papicu`, `Montese`, `Messejana`), `ETA_BY_NEIGHBORHOOD` e `PRODUCTS` (≥16, slugs únicos, todas as categorias cobertas, `priceCents`, `unit`); exportar em `data/index.ts`. Verificar com um script rápido que os slugs são únicos e cada categoria tem ≥1 produto.
- [x] 5.2 Criar `modules/catalog/data/use-storefront.hook.ts` (`'use client'`): lê `bairro`/`categoria` de `useSearchParams` (padrões `Aldeota`/`todas`), `setNeighborhood`/`setCategory` via `router.replace(pathname + '?' + params, { scroll: false })`, deriva `served`, `etaMinutes` (null se não atendido), `visibleProducts`, `hubs` (`[hub, bairros[]][]`), `neighborhoods` (atendidos + não atendidos), `isBagOpen`/`openBag`/`closeBag`, `bagItems = []`, `bagCount = 0`. Verificar que `/?bairro=Meireles&categoria=impressão` restaura seleção após reload e que trocar categoria não adiciona entrada no histórico (botão voltar não passa pelos filtros).
- [x] 5.3 Criar `modules/catalog/components/storefront.component.tsx` compondo `StorefrontLayout` (com `StorefrontHeader` no `header`), `CategoryFilters` + `ProductGrid` quando `served`, ou o estado vazio (`h1` em `font-display font-extrabold` "Ainda não chegamos aí." / "Já já<span class=text-brand>.</span>", "Por enquanto atendemos:", hubs em grid com nome em caixa alta e `border-b-2 border-ink`, bairros como botões sublinhados que chamam `setNeighborhood`) e `BagSheet` ligado ao hook; cards com `href` `/p/${slug}?bairro=...&categoria=...`. Criar `pages/storefront.page.tsx` envolvendo o componente em `<Suspense fallback>` com estrutura estática em `text-placeholder`. Atualizar `modules/catalog/index.ts`. Verificar `/`, `/?categoria=papelaria` e `/?bairro=Papicu` contra `Vitrine.dc.html`.
- [x] 5.4 Remover `src/app/page.tsx`, criar `src/app/(public)/page.tsx` renderizando `StorefrontPage`, e confirmar que `(public)/layout.tsx` não foi alterado além do necessário. Verificar que `/` mostra a vitrine, `/principal` continua abrindo a área privada e `/auth` continua sem layout.
- [x] 5.5 Criar `src/app/(public)/p/[slug]/page.tsx` (Server Component): busca em `PRODUCTS` pelo `slug`, `notFound()` se ausente; lê `searchParams` (`bairro`, `categoria`); trilha "vitrine / categoria / nome" com "vitrine" linkando para `/?bairro=...&categoria=...`; nome em `font-display`, `Price`, unidade em `text-muted-foreground` e a frase "Detalhe do produto chega já já.". Verificar que clicar em um card de `/?bairro=Aldeota&categoria=papelaria` abre o placeholder com a query preservada, que "vitrine" volta com a mesma query e que `/p/nao-existe` retorna 404.

## 6. Validação final

- [x] 6.1 Rodar `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend`. Verificar que ambos terminam sem erros e sem aviso de `useSearchParams` fora de `Suspense`.
- [x] 6.2 Rodar `grep -rn "animate-\|shadow-\|rounded-\|gradient\|backdrop-blur\|dark:" apps/frontend/src/shared apps/frontend/src/modules apps/frontend/src/app` e verificar que retorna vazio (exceto `rounded-none`).
- [x] 6.3 Com `npm run dev --workspace=@jaja/frontend`, conferir visualmente contra `openspec/extras/design/Vitrine.dc.html`: `/` (grade 4 colunas em 1280px e 2 em 375px, header com logo/bairro/ETA/sacola), `/?categoria=papelaria` (filtro ativo com sublinhado vermelho), `/?bairro=Papicu` (estado vazio com hubs e sem ETA), sacola abrindo/fechando por clique, overlay e `Escape`, `/p/<slug>` (placeholder com trilha) e `/principal` (shell administrativo flat com item ativo em vermelho). Verificar que nada anima ao carregar e que Tab mostra foco vermelho.
