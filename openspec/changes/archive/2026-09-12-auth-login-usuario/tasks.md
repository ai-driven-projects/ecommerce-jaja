## 1. Backend: confirmar cobertura existente

- [x] 1.1 Subir banco (`npm run db:start --workspace=@jaja/backend`) e backend (`npm run dev --workspace=@jaja/backend`, porta 4000) e reexecutar os passos 1 e 8 de `apps/backend/src/modules/auth/test/auth.integration.http`. Adicionar no cabeçalho do passo 8 um comentário "usa o email registrado no passo 1: registro seguido de login com as mesmas credenciais". Verificar 201 sem corpo no passo 1 e 200 com `user.admin = false` no passo 8; nenhuma outra linha do arquivo alterada (`git diff --stat apps/backend` mostra só esse arquivo com 1 linha).

## 2. Frontend: rota `/checkout`

- [x] 2.1 Adicionar `export const CHECKOUT_ROUTE = '/checkout';` em `apps/frontend/src/shared/navigation/storefront-routes.ts` e tornar exportada a função `welcomeMessage(user, mode)` em `apps/frontend/src/modules/auth/pages/storefront-login.page.tsx` (única mudança nesse arquivo). Verificar que `git diff apps/frontend/src/modules/auth` mostra apenas a palavra `export` adicionada e que `npm run lint --workspace=@jaja/frontend` passa.
- [x] 2.2 Criar `apps/frontend/src/modules/orders/pages/checkout.page.tsx` com `CheckoutPage` (`'use client'`): `<main className="flex flex-1 flex-col px-6 py-8 md:py-10">` com um componente interno dentro de `Suspense` (fallback: skeleton estático com título e réguas em `text-placeholder`/`border-placeholder`, sem shimmer). O interno usa `useAuth()` e `useStorefront()`; sem sessão renderiza `<h1 className="font-display text-3xl font-extrabold leading-tight tracking-tight md:text-5xl">Para fechar o pedido, entre ou crie sua conta<span className="text-brand">.</span></h1>` e, abaixo, `Card`/`CardContent` com `<AuthForm onSignedIn={(user, mode) => toast.success(welcomeMessage(user, mode))} />` (sem `router`, sem `useEffect`). Com sessão renderiza o título "Checkout chega já já." (ponto em `text-brand`), `user.name`, `user.email` e `<Link href={`${STOREFRONT_ROUTE}?${buildStorefrontQuery(neighborhood, category)}`}>voltar para a vitrine</Link>`. Exportar em `apps/frontend/src/modules/orders/index.ts`. Verificar com `npx tsc --noEmit -p apps/frontend` (ou o build da tarefa 4.1) que compila e que o arquivo não importa `useRouter`.
- [x] 2.3 Criar `apps/frontend/src/app/(public)/checkout/page.tsx` como wrapper de uma linha renderizando `CheckoutPage` (mesmo padrão de `(public)/entrar/page.tsx`). Verificar, com `npm run dev --workspace=@jaja/frontend`, que `/checkout?bairro=Aldeota&categoria=todas` sem cookie mostra o cabeçalho da loja com "Aldeota", o título e as abas "Entrar"/"Criar conta", sem redirecionar.

## 3. Frontend: sacola ligada ao checkout

- [x] 3.1 Em `apps/frontend/src/modules/catalog/components/storefront-shell.component.tsx` (`ConnectedHeader`): importar `useRouter` de `next/navigation`, `CHECKOUT_ROUTE` e `buildStorefrontQuery`; trocar `onCheckout={noop}` por um handler que chama `storefront.closeBag()` e depois `router.push(`${CHECKOUT_ROUTE}?${buildStorefrontQuery(storefront.neighborhood, storefront.category)}`)`. Manter `onChangeQuantity={noop}` e não tocar em `BagSheet`. Verificar por leitura do diff que só esse handler e os imports mudaram e que `git diff --stat apps/frontend/src/shared/components/store` está vazio.

## 4. Validação

- [x] 4.1 Rodar `npm run lint --workspace=@jaja/frontend` e `npm run build --workspace=@jaja/frontend`. Verificar zero erros e que a rota `/checkout` aparece na lista de rotas do build.
- [x] 4.2 Com banco, backend e frontend no ar, validar manualmente em `/checkout?bairro=Aldeota&categoria=todas`: criar conta com dados válidos exibe "Conta criada. Bem-vindo, <nome>", a URL não muda, o conteúdo passa a "Checkout chega já já." com nome e email e o cabeçalho mostra "olá, <nome>"; senha fraca, confirmação divergente e email já cadastrado exibem as mesmas mensagens de `/entrar`; "sair" no cabeçalho exibe "Até já já." e devolve o formulário na mesma URL; reload com sessão mantém o estado autenticado; "voltar para a vitrine" leva a `/?bairro=Aldeota&categoria=todas`. Verificar também que `/entrar?voltar=/` e `/admin/login` (com `usuario@formacao.dev` / `#Senha123`) continuam funcionando como antes.
