// Injection token of the `GeocodingProvider` chosen in `StoresModule`: interfaces
// do not exist at runtime, so Nest needs an explicit token.
export const GEOCODING_PROVIDER = 'GEOCODING_PROVIDER';
